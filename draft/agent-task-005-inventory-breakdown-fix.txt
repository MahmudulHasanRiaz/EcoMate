UPDATE P03h - Fix inventory breakdown totals + product/variant names/images

Scope: ONLY fix items listed here. Keep changes minimal. No refactors.

Issues reported:
- Parent row "Details/Breakdown" shows wrong totals (mixes other items).
- Product name shows variant name (e.g., "42") in main list.
- Variant rows sometimes show "Variant" or attributes only.
- Product images 404 / wrong.

1) Fix getInventoryPaginated filters (productId + variantId)
Files:
- src/server/modules/inventory.ts
- src/app/api/inventory/route.ts
- src/services/inventory.ts

Changes:
- Add `variantId?: string` to GetInventoryParams + service getInventory() + API GET /api/inventory.
- In API route: parse `variantId` from query and pass to server.
- In server getInventoryPaginated:
  - If productId provided: set productWhere.id = productId.
  - If variantId provided: add to invWhere.variantId = variantId.
  - Keep existing search/status/location filters.

Result: /api/inventory?productId=...&variantId=... returns ONLY that product/variant.

2) Fix product/variant name + image mapping
File: src/server/modules/inventory.ts

Add helpers in this file:
- parseProductImage(imageString): string | undefined
  - If JSON array string -> first url.
  - Else if string -> use as-is.
- normalizeUrl(url): string | undefined
  - If empty -> undefined.
  - If starts with http(s) or '/' -> return.
  - Else prefix '/'.

Update product query:
- In prisma.product.findMany select, include `image` and `sku`.

Update inventory item include:
- include variant.image and variant.attributes.

When building map entry:
- productName: product.name (NOT variant name)
- variantName: row.variant?.name ?? undefined
- productSku: product.sku
- variantSku: row.variant?.sku ?? undefined
- productImage: normalizeUrl(parseProductImage(product.image))
- variantImage: normalizeUrl(row.variant?.image) || productImage
- variantAttributes: row.variant?.attributes ?? undefined
- sku: row.variant?.sku || product.sku || 'N/A' (keep)

3) Update InventoryItem type
File: src/types/index.ts
Add optional fields:
- productSku?: string;
- variantSku?: string;
- productImage?: string;
- variantImage?: string;
- variantAttributes?: Record<string, string>;

4) Client display fixes
File: src/app/dashboard/inventory/client-page.tsx

A) productsFromItems map:
- Use item.productName + item.productSku.
- Use item.productImage for product image.
- Variant name fallback:
  - If item.variantName exists -> use it.
  - Else if item.variantAttributes -> format like "Size: 42, Color: Red".
  - Else if item.variantSku -> use that.
  - Else "Variant".
- Variant sku display should use variantSku if present.
- Variant image should use item.variantImage || item.productImage.

B) InventoryProductPickerDialog display line:
- Replace broken joiner with " - ".
- For SKU line, use variantSku if present; else productSku/sku.

5) Breakdown totals (parent vs variant)
File: src/app/dashboard/inventory/client-page.tsx

A) Add helper:
- formatVariantAttributes(attrs) => "Key: Value, ...".

B) Parent row breakdown should be product-level:
- Add `openBreakdownForProduct(productId)` that builds a selectedItem with:
  - productId set
  - variantId = undefined
  - sku = productsFromItems.get(productId)?.sku || ''
  - other fields copied from any existing item for type safety.
- Use this for parent "Details/Breakdown" action.
- Variant rows keep existing breakdown (variantId set).

C) Breakdown fetch should aggregate by location:
- Call /api/inventory with productId and (if variantId) variantId.
- Build a map: locationName => sum(quantity).
- Set breakdownItems from that map (one row per location).
- Optional: add a "Total" row at bottom (sum of all locations).

Deliver back:
- Summary + files touched
- Manual verify:
  - Inventory list shows correct product names (no "42" as product).
  - Variant rows show meaningful names (not just "Variant").
  - Images load or fall back to placeholder.
  - Breakdown for parent equals sum of its variants (per location).
