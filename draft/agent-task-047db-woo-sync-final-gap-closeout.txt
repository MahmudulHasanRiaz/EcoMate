P47db - Woo Sync Final Gap Closeout (No Leftover)

Context
- P47da core fixes are mostly done.
- Remaining regression found: API parses `since` but sends `undefined` to `importWooOrders`.
- This silently breaks custom date-based sync requests.

Scope (strict)
- src/app/api/orders/import/woo/route.ts
- src/app/dashboard/settings/integrations/page.tsx
- src/server/modules/woo-sync.ts (only if needed for backward-compatible response fields)

Do NOT change any other files.

--------------------------------------------------
1) Fix critical regression: preserve `since`
--------------------------------------------------
File: src/app/api/orders/import/woo/route.ts

Current bug:
- `importWooOrders(integrationId, undefined, days, status, forceInline)`

Required fix:
- Pass `since` exactly as parsed:
  `importWooOrders(integrationId, since, days, status, forceInline)`

Also clean response contract:
- Use one consistent API shape.
- Preferred: `return apiSuccess(result);`
- If using raw `NextResponse.json`, remove unused imports and keep shape stable.

Acceptance:
- `since` must no longer be dropped.

--------------------------------------------------
2) Complete manual sync UX metrics (as previously requested)
--------------------------------------------------
File: src/app/dashboard/settings/integrations/page.tsx

Required toast payload usage:
- Show: `fetched`, `imported`, `skipped`, `failed`, `queued`.
- If `failed > 0`, show first 3 `failedIds` in description.

Example description format:
- "Scanned X. Imported Y, Skipped Z, Failed N, Queued Q. Failed IDs: a,b,c"

Keep:
- `forceInline: true`
- `status: 'processing'`

--------------------------------------------------
3) Optional compatibility field (only if existing consumers need it)
--------------------------------------------------
File: src/server/modules/woo-sync.ts

If any existing consumer still reads `updated`, include backward-compatible mapping:
- `updated = imported`

Do not reintroduce fake metrics.

--------------------------------------------------
4) Mandatory verification (must attach proof)
--------------------------------------------------
A) Type safety
- `npx tsc --noEmit --pretty false`

B) Prove `since` now works
- Trigger import API once with explicit `since` and NO `days`.
- Confirm server log shows that same `FinalSince` value.

C) Production crash check
- `docker compose logs worker --since=20m | grep -E "incrementalCache missing in unstable_cache|WOO_BATCH_ITEM_FAIL" | tail -n 50`
- Must not show new incrementalCache crash.

D) DB truth check after manual sync
1. `docker compose exec postgres psql -U postgres -d fashionary -P pager=off -c "select count(*) as woo_last_10d from \"Order\" where source='woo' and \"createdAt\" >= now() - interval '10 days';"`
2. `docker compose exec postgres psql -U postgres -d fashionary -P pager=off -c "select id, \"orderNumber\", status, \"createdAt\" from \"Order\" where source='woo' order by \"createdAt\" desc limit 30;"`

--------------------------------------------------
5) Required output format
--------------------------------------------------
Return exactly:
1. Files changed
2. Diff snippet for `since` fix in route.ts
3. One real sync response JSON sample (with fetched/imported/skipped/failed/queued/failedIds)
4. Worker log proof snippet (no incrementalCache crash)
5. DB query outputs (count + latest rows)
6. `npx tsc --noEmit` result
