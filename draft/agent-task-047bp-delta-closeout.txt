P47bp Delta Closeout - Must-Fix Remaining Gaps (No Miss)

Goal
- Fully close remaining accounting-connectivity risks found in review.
- Keep changes minimal and targeted.
- No commit/push steps here.

Files in scope
- src/app/api/check-passing/route.ts
- src/app/api/staff/route.ts
- src/app/api/staff/[id]/route.ts
- src/server/modules/analytics.ts
- src/server/modules/staff-payment-ledger.ts (track this file)

--------------------------------------------------------------------
1) CRITICAL: Use domain reconcile for Purchase status updates in check-passing route
--------------------------------------------------------------------

Problem
- In Purchase branch, route still does direct `tx.purchasePayment.update(...)`.
- This bypasses `updateCheckStatusCore(...)` reconciliation.

Required
1. Refactor PATCH flow in `src/app/api/check-passing/route.ts`:
   - Do NOT keep whole `updates[]` under one single `prisma.$transaction(...)`.
   - Process updates sequentially (one-by-one).
2. Purchase source:
   - Call `updateCheckStatusCore({ paymentId: update.id, status, user: actor.name })`.
   - If result is not success, throw with source/id context.
   - Do not do additional direct purchasePayment status update in route.
3. Expense source:
   - Continue using `updateExpenseCheckStatus(...)`.
4. Staff source:
   - Continue using `updateStaffPaymentCheckStatus(...)`.
5. CheckPassingItem refresh:
   - Keep for Expense + Staff in route.
   - For Purchase, do not duplicate if already handled by core.
6. CheckPassingLog:
   - Keep log creation with previous/new status.
   - Ensure logs still write even after removing the global transaction pattern.

Acceptance
- Status updates from check-passing screen now reconcile Purchase ledger/payment status via core logic.

--------------------------------------------------------------------
2) HIGH: Fix staff list totalPaid fallback bug (0 cleared should stay 0)
--------------------------------------------------------------------

Problem
- In `src/app/api/staff/route.ts`, `mapStaff(...)` falls back to raw payment sum when `paidTotal` is 0.

Required
1. Update `mapStaff(...)` signature:
   - Make `paidTotal` optional (`paidTotal?: number`).
2. Total logic:
   - If `paidTotal` is provided (including 0), use it directly.
   - Only fallback to raw sum when `paidTotal` is undefined.
3. Keep response shape unchanged.

Acceptance
- Staff with only pending checks shows cleared `totalPaid = 0` (not inflated).

--------------------------------------------------------------------
3) HIGH: Staff details endpoint must always output cleared-funds totals
--------------------------------------------------------------------

Problem
- In `src/app/api/staff/[id]/route.ts`, includeHistory=true path still ends up with raw `member.payments` totals inside `mapStaff`.

Required
1. Keep existing cleared-funds computation (`totalPaid` variable).
2. After `mapped = mapStaff(...)`, always override:
   - `mapped.financials.totalPaid = totalPaid`
   - `mapped.financials.dueAmount = Math.max(0, incomeTotal - totalPaid)`
3. Do this for both includeHistory true/false.

Acceptance
- `/api/staff/:id` totals are cleared-funds consistent in every mode.

--------------------------------------------------------------------
4) HIGH: Prevent analytics staff-payment double counting
--------------------------------------------------------------------

Problem
- `src/server/modules/analytics.ts` now adds staff cleared amounts on top of `expenseAgg`.
- If staff payment is also represented in `Expense`, this can double count.

Required
1. Staff payment queries in analytics must include only unlinked staff payments:
   - `where: { ...whereStaffPayment, Expense: { is: null } }`
   - Apply to summary query and monthly query.
2. Keep existing cleared-funds formula for staff payments.
3. Keep expense aggregation as-is.

Acceptance
- No duplicate expense impact when staff payment has linked Expense row.

--------------------------------------------------------------------
5) CRITICAL: Ensure new ledger module is tracked
--------------------------------------------------------------------

Required
- Confirm `src/server/modules/staff-payment-ledger.ts` is tracked (not untracked).

Acceptance
- `git status --short` does not show `?? src/server/modules/staff-payment-ledger.ts`.

--------------------------------------------------------------------
6) Verification (mandatory)
--------------------------------------------------------------------

A) Static
1. `git status --short`
2. `npx tsc --noEmit`

B) Route behavior checks
1. Purchase pending check -> Passed from check-passing UI/API
   - Confirm PO/step payment status and ledger update (via existing purchase core behavior).
2. Staff pending check only
   - `GET /api/staff` shows staff `totalPaid` unchanged (0 if no cleared component).
   - `GET /api/staff/{id}` shows same cleared total.

C) Analytics double count check
1. Create/identify one staff payment linked to Expense and one unlinked.
2. `GET /api/analytics`:
   - linked one must not be counted twice.
   - unlinked one counted only by cleared portion.

D) Quick SQL spot checks
1. `select accountId, debit, credit from "LedgerEntry" where "postingGroup"='staffPayment:<PAY_ID>';`
2. `select id, "checkStatus", amount, "check" from "StaffPayment" where id='<PAY_ID>';`

Expected output to report back
- changed files list
- pass/fail for A/B/C/D
- any failing command/output snippet
