P47dm - Print Hardening + Status Matrix Audit + Courier Auto-Status Guard

Context
- This is a follow-up after P47dk (and partial P47dl).
- Keep existing working functionality intact:
  - Woo webhook auto-ingest
  - publish-mode stock guard
  - order lock + optimistic concurrency + realtime sync
- Do NOT revert prior changes.
- Target: close remaining gaps with minimal, safe, testable edits.

==================================================
0) Current Audit Summary (Verified Now)
==================================================

Completed and working:
- `updateOrderSchema` now accepts both display + internal status values.
- Tokenized bulk print flow exists (`/api/print/bulk` + `/print/bulk?token=...`).
- Type-check is currently passing.

Still incomplete / risky:
1) Large print warning state exists but no dialog is actually rendered.
   - File: `src/app/dashboard/orders/client-page.tsx`
   - `printWarning` is set, but no `AlertDialog` wired to it.

2) Print token routes still use auth-only checks (no explicit `orders:read` permission gate).
   - Files:
     - `src/app/api/print/bulk/route.ts`
     - `src/app/api/print/bulk/[token]/route.ts`

3) Bulk print page still does N-per-order fetch (`Promise.all(getOrderById(...))`).
   - File: `src/app/print/bulk/client-page.tsx`
   - This can still choke on very large batches.

4) Latent status-action bug:
   - `statusMap.rts = 'ReadyToShip'` is not canonical.
   - File: `src/server/modules/orders.ts`
   - `normalizeStatusInput('ReadyToShip')` does not map cleanly by label map.

5) Courier auto-status mapping risk (Pathao/Carrybee):
   - Mapping returns display labels like `In-Courier` / `Return Pending`.
   - These are written directly as `order.status` with `as any`.
   - Files:
     - `src/server/modules/courier/pathao.ts`
     - `src/server/modules/courier/carrybee.ts`
   - This is brittle against Prisma enum key expectations and can silently regress.

6) "Rows per load" entry UX mismatch risk:
   - Backend clamps to `MAX_PAGE_SIZE=100`, but UI input allows arbitrary values.
   - User can type very large values; effective query and visible input can diverge.
   - Files:
     - `src/app/dashboard/orders/client-page.tsx`
     - `src/lib/pagination.ts` (already clamps correctly)

==================================================
1) Fix A - Complete Print Hardening (finish P47dl properly)
==================================================

Files:
- `src/app/dashboard/orders/client-page.tsx`
- `src/app/api/print/bulk/route.ts`
- `src/app/api/print/bulk/[token]/route.ts`
- `src/app/print/bulk/client-page.tsx`
- (new) `src/app/api/print/bulk/orders/route.ts`

Required changes:
1. Render the missing warning dialog:
   - Wire `printWarning.isOpen` to an `AlertDialog`.
   - Message: large batch may be slow, confirm continue.
   - Continue calls `executeBulkPrint(printWarning.type!)`.
   - Cancel closes dialog cleanly.

2. Permission gate:
   - Add `enforcePermission('orders', 'read')` to both print token routes.
   - Keep existing behavior for authenticated/authorized users.

3. Replace N+1 fetch on print page:
   - Add bulk payload endpoint: `POST /api/print/bulk/orders`.
   - Input: `{ ids: string[] }`, max 2000, enforce `orders:read`.
   - Return only fields needed by `InvoiceTemplate`/`StickerTemplate`.
   - Use single request in `src/app/print/bulk/client-page.tsx`.
   - Keep old per-order fetch as fallback only.

4. Optional but recommended:
   - If selected IDs exceed 2000 on client, block early with clear toast.

Acceptance:
- >300 selection shows warning before open.
- 120+ print opens with `token=...` and loads from one primary data call.
- Unauthorized users cannot create/retrieve/read print batches.

==================================================
2) Fix B - Status Matrix Deep Audit + Close Remaining Gaps
==================================================

Files:
- `src/server/modules/orders.ts`
- `src/lib/order-status-flow.ts` (only if needed for consistency)
- `src/lib/validations/orders.ts` (only if needed)
- (new) `scripts/verify-order-status-matrix.ts`

Required changes:
1. Fix latent `rts` action mapping:
   - In `statusMap`, replace `rts: 'ReadyToShip'` with canonical internal status key:
     - `rts: 'RTS__Ready_to_Ship_'`

2. Verify all status inputs accepted where intended:
   - Build script `scripts/verify-order-status-matrix.ts`:
     - check each `ORDER_STATUSES` value validates in `updateOrderSchema`.
     - check display aliases used in transitions also validate.
     - fail with non-zero exit on any mismatch.

3. Do NOT expand schema to arbitrary strings.
   - Keep strict whitelist behavior.

Acceptance:
- No "validation failed" for any legitimate status from UI flows.
- `rts` bulk action path is canonical and stable.

==================================================
3) Fix C - "Rows per load" Entry Hardening (User-Facing)
==================================================

Files:
- `src/app/dashboard/orders/client-page.tsx`

Required changes:
1. Add explicit client-side bounds:
   - min = 1
   - max = 100 (must match backend clamp)

2. Prevent input/value mismatch:
   - If user enters >100, clamp to 100 in UI.
   - If invalid/empty input, restore previous valid value.

3. Reduce noisy re-fetch on typing:
   - Use a local draft input state and apply on blur/Enter OR debounce updates.
   - Do not trigger API fetch on every keypress for this field.

Acceptance:
- User-entered page size stays consistent with effective backend page size.
- No confusing case where UI shows huge value but server serves 100.

==================================================
4) Fix D - Courier Auto-Status Sync Guard (Pathao/Carrybee)
==================================================

Files:
- `src/server/modules/courier/pathao.ts`
- `src/server/modules/courier/carrybee.ts`
- (new shared helper optional) `src/lib/order-status-normalize.ts` OR export from orders module safely
- (new) `scripts/verify-courier-status-mapping.ts`

Required changes:
1. Normalize courier-mapped statuses to canonical internal enum keys before DB update:
   - Convert mapped results (`In-Courier`, `Return Pending`, etc.) to internal keys
     (`In_Courier`, `Return_Pending`, etc.) before writing `order.status`.

2. Ensure both webhook and sync paths use the same normalization logic:
   - Pathao webhook handler
   - Pathao refresh/sync handler
   - Carrybee webhook handler

3. Keep current business logic intact:
   - existing stock deduction/restore triggers
   - existing log creation
   - existing signature checks

4. Add lightweight verification script:
   - `scripts/verify-courier-status-mapping.ts`
   - assert common incoming courier states/events resolve to valid internal status keys.

Acceptance:
- No Prisma enum runtime risk from display-label status writes.
- Courier-driven auto status update remains stable and future-safe.

==================================================
5) Verification Commands
==================================================

Run from:
```powershell
cd E:\fashionary\Fashionary
```

1) Type-check
```powershell
npx tsc --noEmit --pretty false
```

2) Status matrix script
```powershell
npx tsx scripts/verify-order-status-matrix.ts
```

3) Courier mapping script
```powershell
npx tsx scripts/verify-courier-status-mapping.ts
```

4) Existing webhook checks (regression)
```powershell
npx tsx scripts/check-woo-hook-details.ts
npx tsx scripts/check-woo-deliveries.ts
```

5) Manual checks
- Orders page: set rows-per-load to `150`, ensure UI clamps to `100`.
- Bulk status: confirm `Confirmed -> RTS` works (no validation error).
- Bulk print:
  - Select >300 orders -> warning dialog appears.
  - Select 120+ orders -> opens token URL, batch data loads reliably.
- Courier auto-status:
  - Trigger one Pathao/Carrybee webhook sample event and confirm order status updates without enum/runtime error.

==================================================
6) Required Output
==================================================

Return exactly:
1. Files changed
2. Diff snippets for:
   - print warning dialog render
   - print permission guards
   - bulk print single-fetch endpoint/page wiring
   - `statusMap.rts` canonical fix
   - courier status normalization for Pathao/Carrybee
   - rows-per-load input hardening
3. `npx tsc --noEmit --pretty false` result
4. Script outputs:
   - `verify-order-status-matrix.ts`
   - `verify-courier-status-mapping.ts`
   - webhook check scripts
5. Manual results (rows-per-load, bulk RTS, bulk print, courier event)
6. Residual limits/risks
