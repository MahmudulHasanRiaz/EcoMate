UPDATE P22b - Export CSV queue fixes (permissions + worker + migration)

Scope: ONLY export feature. Keep changes minimal. No refactors.

Findings:
- export API routes call enforcePermission(req, ...) which is wrong for current signature.
- report worker still has TODO; orders-export job is not processed.
- migration for ExportJob not created yet.

1) Fix enforcePermission usage (all export routes)
Files:
- src/app/api/exports/orders/route.ts
- src/app/api/exports/[id]/route.ts
- src/app/api/exports/[id]/download/route.ts

Change to:
```
const { allowed, error, staff } = await enforcePermission('orders', 'read');
if (!allowed) return error;
```
Remove the `req` argument entirely.

2) Implement report worker job
File: scripts/bullmq-worker.ts
Replace the reportWorker TODO block with real handler:
```
const reportWorker = new Worker(
  'reports',
  async (job) => {
    if (job.name === 'orders-export') {
      const { jobId, orderIds, format } = job.data as { jobId: string; orderIds: string[]; format: string };
      const { markExportProcessing, markExportFailed, generateOrdersCsv } = await import('@/server/modules/exports');
      await markExportProcessing(jobId);
      try {
        await generateOrdersCsv({ orderIds, format, jobId });
        return { ok: true };
      } catch (err) {
        await markExportFailed(jobId, String(err));
        throw err;
      }
    }
    return { ok: false, reason: 'unknown_job' };
  },
  { connection, concurrency: 2 }
);
```
Keep other workers unchanged.

3) Migration for ExportJob model
Run:
```
npx prisma migrate dev -n add_export_jobs
npx prisma generate
```
If migrate dev fails due to drift:
```
npx prisma migrate dev -n add_export_jobs --create-only
npx prisma migrate deploy
npx prisma generate
```
Deliver migration folder name.

Deliver back:
- Summary + files touched
- Migration folder name
- Manual verify:
  - POST /api/exports/orders returns jobId
  - Worker processes orders-export and completes job
  - GET /api/exports/{id} returns Completed
  - Download works
