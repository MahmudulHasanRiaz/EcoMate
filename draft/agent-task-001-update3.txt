UPDATE 3 - Remaining gaps after your latest completion

Scope: ONLY fix the items listed here. Do not refactor.

1) BUG in handleSaveCuttingStep (EXTERNAL branch)
File: src/app/dashboard/purchases/[id]/client-page.tsx
- You define `dmg` but return `damageQty: cDmg` (undefined) -> runtime error.
- Fix to use `const cDmg = Number(it.cuttingDamagedQty) || 0;` and pass `damageQty: cDmg`.
- Billable MUST use printing + cutting damage:
  billable = max(0, qty - printingDamagedQty - cuttingDamagedQty)
  Apply this in external branch (internal already ok).

2) Input max limits for damage/wastage (mobile + desktop)
File: src/app/dashboard/purchases/[id]/client-page.tsx
- Printing damage input (mobile): add min=0, max=qty.
- Cutting damage input (mobile + desktop): add min=0, max=(qty - printingDamagedQty).
- Finishing wastage input (mobile + desktop): add min=0, max=(qty - printingDamagedQty - cuttingDamagedQty).

3) Invoice meta still uses legacy yard fields (remove)
File: src/app/dashboard/purchases/[id]/client-page.tsx
- Remove `damagedYards` and `extraWastage` variables and any meta rows using them.
- Replace with pcs-based totals computed from items:
  printingDamageTotal = sum(printingDamagedQty)
  cuttingDamageTotal = sum(cuttingDamagedQty)
  finishingWastageTotal = sum(finishingWastageQty)
- Show ONLY the current step's damage prominently on its invoice:
  - PRINTING: show printingDamageTotal (pcs)
  - CUTTING: show cuttingDamageTotal (pcs)
  - FINISHING: show finishingWastageTotal (pcs)
- Optional: if you include other-step damage, keep it a single compact line and only when value > 0.

4) Cutting invoice line math confirmation
File: src/app/dashboard/purchases/[id]/client-page.tsx
- Ensure cutting line total + ?for X pcs? uses billable = qty - printingDamagedQty - cuttingDamagedQty everywhere.
  (Some places already updated; double-check while editing.)

Deliver back: summary of changes + tests run.
