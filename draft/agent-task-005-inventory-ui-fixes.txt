UPDATE P03e - Inventory UI/UX fixes (product display, variable+combo, stock movement picker)

Scope: ONLY items listed here. Keep changes minimal. No refactors unrelated to inventory.

1) Stock Movement product picker UI (Adjust + Transfer)
File: src/app/dashboard/inventory/client-page.tsx
Goal: Replace the current Product dropdown with a purchase-style searchable list UI (like purchase product add dialog), not the small combobox.

Implementation:
- Create a small internal component in this file: `InventoryProductPickerDialog`
  - Layout: same style as `src/components/purchase-product-add-dialog.tsx` (search input + scroll list + cards).
  - Data source: use `getInventory({ search, locationId, status: 'all', pageSize: 50, cursor })`.
  - List item card shows: productName, variantName (if any), sku, locationName, available qty.
  - Allow selecting a row; on select:
    - set productId, variantId, inventoryItemId, locationId in adjustmentData/transferData.
    - close dialog.
  - Add "Load more" when nextCursor exists.
- Replace the Product `<Select>` in Adjust + Transfer tabs with a button that opens this dialog.
- Keep current variant + lot selects; they should be auto-populated after selection.

Expected: product picking feels like purchase product selection (search + list), not the tiny select.

2) Inventory list: product names/images missing + variable expand/collapse broken
Files:
- src/server/modules/inventory.ts
- src/services/inventory.ts (types only if needed)
- src/app/dashboard/inventory/client-page.tsx

Fixes:
A) Server include product/variant images in inventory items:
  - In getInventoryPaginated, when building map entries, include:
    - productImage (from product.image JSON, use first image url if available)
    - variantImage (from variant.image if available, else product image)
    - productName already present; ensure sku uses product sku when no variant.
  - Add these fields to the item object returned to client.

B) Client should NOT rely on allProducts for names/images/variants:
  - Build `productsFromItems` map from the current `items` array:
    - productId -> { name: item.productName, sku: item.sku, image: item.productImage }
    - variants list = unique by variantId using item.variantName, item.sku, item.variantImage.
  - Replace all `productById` usage in list rendering with `productsFromItems` map.
  - `isExpandable` should be true when variants list length > 0.
  - Variant rows should come from this variants list (not product.variants).
  - `getProductImage` / `getVariantImage` should use these maps.
  - Fix `findItemForVariant` to fallback to first item for product if no base row:
    - return itemsForProduct.find(i => !i.variantId) ?? itemsForProduct[0];

Result: product names/images render correctly; variable expand/collapse works again.

3) Combo products section (separate, accurate)
Files:
- src/app/api/products/route.ts
- src/services/products.ts
- src/app/dashboard/inventory/client-page.tsx

Goal: restore combo stock section with correct availability and filters.

API changes:
- Add optional query params to /api/products:
  - type=combo -> filter prisma productType to combo only.
  - locationId -> when computing inventory availability, filter inventoryItem by locationId if provided.
- Keep defaults (no filter) unchanged for other modules.

Client changes:
- Add `getComboProductsPaginated` in services/products.ts (wrapper for /api/products with type=combo, locationId, search, cursor, pageSize).
- In inventory client page:
  - Fetch combo list separately using locationFilter + searchTerm.
  - Apply statusFilter on combos:
    - active: inventory > 0
    - low-stock: inventory > 0 && inventory <= lowStockThreshold
    - all: no filter
  - Replace existing comboStocks client calc with server-provided combos.

4) Actions menu missing (when variable product)
File: src/app/dashboard/inventory/client-page.tsx
- With item map fix above, ensure actions menu still shows View Movement / Lots / Details.
- If product row has no base inventory item, use first variant item as fallback (see step 2B).

Deliver back:
- Summary + files touched
- Manual verify:
  - Stock Movement picker works with search + load more; selection fills product/lot/location.
  - Inventory list shows correct product names/images.
  - Variable products expand to variants again.
  - Combo section appears and respects filters.
