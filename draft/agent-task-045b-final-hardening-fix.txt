UPDATE P45b - Final Hardening Fixes (finish gaps)

Scope: partners async UX + PM2 log retention + Prisma config deprecation. Keep changes minimal.

1) Partners async select: add cursor + load more (true async)
File: src/components/partner-async-select.tsx
- Add state: nextCursor, hasMore, isLoadingMore.
- When popover opens OR query changes:
  - reset options to initialOptions
  - reset nextCursor
  - fetch first page (pageSize=20, cursor=undefined, search=query, type=additionalParams?.type)
- Use API response fields: items, nextCursor, hasMore
- Add Load More button at bottom of CommandList when hasMore
  - loadMore() fetches with cursor=nextCursor and appends items (dedupe by id)
- Ensure existing selected value still renders (use initialOptions for current selection)

2) Purchases page: keep small initial options but remove any heavy prefetch claim
File: src/app/dashboard/purchases/[id]/page.tsx
- Keep getSuppliers/getVendors default batch (pageSize 20) OR reduce to 10 (optional)
- Do NOT fetch pageSize=1000 anywhere
- Update comments/notes if needed (no claims about removing prefetch)

3) Partners API: ensure vendor type filtering is supported (already added)
- Verify /api/partners/vendors supports ?type=
- No change if already done.

4) PM2 log retention (actual config + commands)
File: ecosystem.config.cjs
- Add log_date_format to BOTH apps (web + worker): 'YYYY-MM-DD HH:mm:ss Z'
- Keep existing values.
Commands (run on server/local):
- pm2 install pm2-logrotate
- pm2 set pm2-logrotate:max_size 10M
- pm2 set pm2-logrotate:retain 30
- pm2 set pm2-logrotate:compress true
- pm2 set pm2-logrotate:dateFormat YYYY-MM-DD_HH-mm-ss
- pm2 set pm2-logrotate:rotateInterval '0 0 * * *'
- pm2 set pm2-logrotate:rotateModule true
- pm2 save
Save command output to: draft/pm2-log-rotate.txt (overwrite)

5) Prisma config deprecation fix (attempt)
- Create prisma.config.ts with:
  import { defineConfig } from 'prisma/config';
  export default defineConfig({
    schema: 'prisma/schema.prisma',
    seed: 'tsx prisma/seed.ts',
  });
- Remove "prisma" field from package.json
- Run: npx prisma generate
If Prisma CLI fails to parse config:
  - Revert prisma.config.ts and restore package.json prisma field
  - Record blocker in draft/task.txt (Status: BLOCKED-ENV) with exact error

6) Verification
- npm run build
- Save output to draft/p45b-build.txt

7) Tracker update
- draft/task.txt:
  - Keep P45 DONE-VERIFIED only if all above are completed
  - If Prisma config blocked, note it explicitly
  - Add evidence: draft/pm2-log-rotate.txt, draft/p45b-build.txt

Deliver back:
- Summary + files touched
- draft/pm2-log-rotate.txt
- draft/p45b-build.txt
- Updated draft/task.txt
