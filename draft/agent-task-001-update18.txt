UPDATE 18 - Invoice lots layout + image fallback + note placement + internal cutting rate display

Scope: ONLY items listed here. Keep changes minimal.

1) Invoice lots layout (clear, compact sub-line per product)
File: src/app/dashboard/purchases/[id]/client-page.tsx
Section: handleGenerateWorkOrder -> formatLots + itemsHtml

Goal: inside each product cell, render a clean sub-line with a mini grid/table
so lots are easy to read at a glance.

Implementation:
- Replace current formatLots() output with a compact nested table:
  Example HTML:
  <div style="margin-top:4px;border:1px dashed #e2e8f0;border-radius:4px;padding:4px;">
    <table style="width:100%;border-collapse:collapse;font-size:10px;color:#475569;">
      <tr>
        <td style="font-weight:600;">Lot</td>
        <td style="font-weight:600;">J</td>
        <td style="font-weight:600;">O</td>
        <td style="font-weight:600;">S</td>
      </tr>
      <tr>
        <td>LOT123</td><td>2</td><td>1</td><td>0.5</td>
      </tr>
      ...
    </table>
  </div>
- Keep it compact: use small font (10px) + minimal padding (2-4px).
- Group allocations by lot and SUM J/O/S. Only show a part if >0.
- Keep current logic to show lots in all steps when internal fabric is used.

2) Invoice product image fallback (variant -> product)
Files:
- src/services/purchases.ts
- src/app/dashboard/purchases/[id]/client-page.tsx

Fix (server mapping):
- When building purchaseItems.imageUrl, pick the first valid URL:
  variant image if valid, else product image.
- Use resolveImageSrc from src/lib/image.ts to validate/normalize.
- Avoid returning placeholder; if resolveImageSrc returns placeholder, treat as null.

Fix (invoice rendering):
- Use the resolved image URL (already normalized). If null, do NOT render <img>.
- Keep safeImgUrl for absolute URL conversion; also skip if resolved URL equals placeholder.

3) Note field placement (bottom of invoice)
File: src/app/dashboard/purchases/[id]/client-page.tsx

Fix:
- Remove Note rows from metaRows.
- Add a new full-width note box at the bottom (after summary), only if note exists:
  <div class="note-box">
    <div style="font-weight:600;margin-bottom:4px;">Note</div>
    <div>${note}</div>
  </div>
- Add CSS for .note-box: full width, bordered, background white, padding 12px, font-size 12px.

4) Internal Cutting Lumpsum -> auto per-piece rate display
File: src/app/dashboard/purchases/[id]/client-page.tsx

Requirement:
- When Cutting Type = INTERNAL and Lumpsum Amount entered,
  show a read-only "Per-piece rate" below the Lumpsum input:
  perPiece = totalAmount / totalBillableQty (qty - printing damage - cutting damage).
- Display only (read-only helper text). No new inputs, no edit.

Deliver back: summary + quick invoice screenshots/verify notes.
