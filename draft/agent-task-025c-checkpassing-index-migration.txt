UPDATE P25c - Create migration for CheckPassingItem index (missing)

Scope: Prisma migration only. No code edits.

Problem:
- @@index([passingDate, source, id]) added to schema.prisma but no migration exists.
- db push is not acceptable for other envs; need a proper migration folder.

Steps:
1) Ensure dev server stopped (to avoid prisma engine lock).
2) Create migration:
   npx prisma migrate dev -n add_checkpassingitem_index --create-only

3) Apply migration:
   npx prisma migrate deploy

4) Generate client:
   npx prisma generate

Deliver back:
- Migration folder name created
- Output of migrate status
- Confirm generate succeeded

Notes:
- If drift blocks create-only, use diff fallback:
   $stamp = Get-Date -Format yyyyMMddHHmmss
   $folder = "prisma/migrations/${stamp}_add_checkpassingitem_index"
   New-Item -ItemType Directory -Path $folder | Out-Null
   npx prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/schema.prisma --script | Out-File -FilePath "$folder/migration.sql" -Encoding utf8
   npx prisma migrate deploy
   npx prisma generate
