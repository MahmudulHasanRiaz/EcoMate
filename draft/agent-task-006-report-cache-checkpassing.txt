UPDATE P04 - Report caching + Check Passing optimization

Scope: ONLY add caching + reduce heavy queries. No UI redesign.

1) Analytics caching
- File: src/server/modules/analytics.ts + src/app/api/analytics/route.ts
  - Add TTL cache (1–3 min) for analytics payload.
  - Invalidate on write events: orders, expenses, purchases, staff payments.
  - Keep real-time endpoints (stock/action center) uncached.

2) Partners overview caching
- File: src/services/partners.ts (getPartnerOverviewStats)
  - Add TTL cache (1–3 min). Invalidate on purchase/payment changes.
  - Avoid full scan of purchasePayment where possible (use aggregate/groupBy).

3) Courier metrics caching
- File: src/server/modules/courier/reconciliation.ts
  - Add short TTL cache for metrics + returnPending summary.
  - Invalidate on order status changes + courier payment updates.

4) Check Passing heavy query (Decision: denormalized table)
- Create a new table (e.g., CheckPassingItem) with fields:
  id, source, sourceId, passingDate, amount, status, checkNo,
  referenceId, referenceLabel, referenceUrl, payee, type, createdAt, updatedAt.
- Write-time sync:
  - PurchasePayment create/update (check fields) → upsert CheckPassingItem.
  - Expense create/update (check fields) → upsert CheckPassingItem.
  - StaffPayment create/update (check fields) → upsert CheckPassingItem.
  - Status change (Passed/Bounced/Cancelled) updates item.
- Backfill script/migration to populate existing data once.
- API /check-passing should read ONLY from this table with cursor + search (ILIKE on checkNo/referenceLabel/payee/type).

Deliver back
- Summary + files touched
- Manual test: analytics/partners/courier still correct after create/update operations; cache invalidation works.
