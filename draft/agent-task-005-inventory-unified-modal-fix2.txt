UPDATE P03j-3 - Remove leftover breakdown/lots logic + wire Summary data to Stock modal

Scope: ONLY fix items listed here in `src/app/dashboard/inventory/client-page.tsx`. No refactors.

Problem (verified in code):
- `DialogMode` is now `'movement' | 'viewMovement' | 'stock'`, but code still contains:
  - useEffect for `mode === 'breakdown'`
  - useEffect for `mode === 'lots'`
  - `openDialog` pre-seed for `mode === 'lots'`
  - `lotRows` selector allows `mode === 'lots'`
  - DialogContent width uses `mode === 'lots'`
  - Summary UI reads `breakdownData`, but Stock fetch effect writes to `breakdownItems`
=> Summary tab can be empty/stale and legacy modes still exist.

Required fixes:
1) Delete legacy effects and mode branches
- Remove the `useEffect` that runs only when `dialogState.mode === 'breakdown'`.
- Remove the `useEffect` that runs only when `dialogState.mode === 'lots'`.
- Remove the `if (mode === 'lots' && item)` pre-seed block in `openDialog`.
- Update `lotRows` memo to only allow `mode === 'stock'` (remove `mode === 'lots'` check).
- Update DialogContent class: replace `dialogState.mode === 'lots' && 'sm:max-w-4xl'` with `dialogState.mode === 'stock' && 'sm:max-w-4xl'`.

2) Use ONE summary state (breakdownData) for Stock Summary tab
- Remove `breakdownItems` state and `breakdownRows` variable (unused).
- In the existing Stock fetch effect (the one that already computes location + variant totals),
  change `setBreakdownItems(...)` to `setBreakdownData(...)`.
- Keep `breakdownLoading` as-is (Summary tab already uses it).
- Keep `variantBreakdownItems` logic unchanged (only for parent view).

3) Sanity
- No code paths should reference `mode === 'breakdown'` or `mode === 'lots'` after this change.
- Summary tab must render using `breakdownData` populated by the Stock effect.

Deliver back:
- Summary + files touched
- Manual verify:
  - Summary tab shows data for parent + variant.
  - No stray “Details / Breakdown” or “View Lots” logic remains.
