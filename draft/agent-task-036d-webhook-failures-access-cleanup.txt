UPDATE P36d - Harden Webhook Failures access gate (admin bypass + cleanup)

Scope: ONLY src/app/dashboard/webhook-failures/page.tsx. Minimal changes.

Issue:
- hasAccess uses permissions?.admin (non-existent).
- Loading guard blocks even when Admin role is known but permissions is null (transient or role mismatch).

Fix:
1) Remove permissions?.admin from hasAccess
- Change:
  const hasAccess = Boolean(isAdmin || permissions?.integrations?.read || permissions?.admin);
  to:
  const hasAccess = Boolean(isAdmin || permissions?.integrations?.read);

2) Loading guard should allow Admin even if permissions null
- Replace:
  if (!isUserLoaded || permissions === null) { ... }
  with:
  if (!isUserLoaded || (!isAdmin && permissions === null)) { ... }

3) fetchFailures should run only when isUserLoaded and hasAccess
- Update useEffect:
  if (isUserLoaded && hasAccess) { fetchFailures(); }

Deliver back:
- Summary + file touched
- Confirm Admin can access even if permissions fails to load

No other changes.
