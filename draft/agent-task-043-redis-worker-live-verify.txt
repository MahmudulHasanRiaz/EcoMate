UPDATE P43 - Live Redis/BullMQ verification (queue path)

Scope: Runtime verification only. No code changes.

Goal:
- Prove that batch sync uses BullMQ worker when Redis is ON
- Prove SMS/Notifications queue-first path works
- Prove report-cache uses Redis (optional log)

Steps:
1) Start Redis locally (or ensure REDIS_URL is set and reachable).
2) Start worker in a separate terminal:
   node scripts/bullmq-worker.ts
   (or npm run worker if exists)
3) Trigger actions:
   a) Woo manual import (should enqueue sync-order-batch)
   b) Send test notification/SMS (if test endpoint exists)
4) Collect evidence:
   - Worker logs showing job processed
   - API logs showing queued=true
5) Write evidence file:
   draft/redis-worker-verify.txt
6) Update draft/task.txt:
   - Add P43 DONE-VERIFIED with evidence file

Deliver back:
- draft/redis-worker-verify.txt
- Updated draft/task.txt
- Note any missing scripts or errors
