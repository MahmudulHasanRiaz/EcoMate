P47 Review – latest claim (still NOT ready)

What’s OK now
- normalizeBdPhone updated to accept 0XXXXXXXXXX and 880/+880 (good).
- IP no longer printed; UI shows “Network Protection” without IP text (good).
- Scope selector added on Order + Customer pages (good).
- /api/woo/* endpoints exist and are API-key protected (good).
- post‑order restriction creation added in /api/woo/incomplete-orders/complete (partial).

Still wrong / missing (must fix)
1) Plugin phone validation is still wrong
   - File: draft/fashionary-woo-plugin/assets/js/checkout.js
   - It still uses regex ^(?:01)[3-9]\d{8}$ (too strict) and ignores config.phoneValidation.
   - Must align with server rule: accept 0XXXXXXXXXX or 880/+880 + 10 digits. Use config.phoneValidation.regex if provided.
   - If invalid: show red warning AND block checkout (not just highlight).

2) Plugin does NOT block checkout properly
   - fashionary-woo-plugin.php uses `throw new Exception($msg)` in checkout_process.
   - Use `wc_add_notice($msg, 'error');` and return/exit. Exception can be swallowed or show generic error.

3) Client IP not sent to server correctly
   - Plugin sends IP in JSON body; server ignores body and reads headers.
   - Must send header `X-Forwarded-For` with real client IP (from $_SERVER['HTTP_CF_CONNECTING_IP'] or $_SERVER['REMOTE_ADDR']).

4) /api/restrictions GET/DELETE not scope‑aware in UI
   - UI checks restrictions with only targetType/targetValue, so any block anywhere shows as “blocked”.
   - For SITE scope, must include integrationId in GET and DELETE filters.
   - Update API to accept integrationId in GET, and update UI to pass it.

5) /api/restrictions POST ignores `message` sent by UI
   - Current API always replaces message with integration/general settings.
   - Decide one: either remove message from UI payload OR allow custom message when provided.

6) Post‑order restriction creation is hacky (upsert with invalid id)
   - File: src/app/api/woo/incomplete-orders/complete/route.ts
   - Replace with proper findFirst/update/create logic (no error‑driven flow).
   - Also avoid creating duplicates if one already exists and still active.

7) Server still has unused duplicate module
   - src/server/modules/woo-public.ts is unused. Remove or refactor APIs to use it.

8) Config vs validation mismatch in plugin
   - plugin-config regex now allows 0 or 880… but checkout.js ignores it.
   - Must load config first and use it for validation + messages.

Deliverables
- Fix all above, then send: file list + snippets proving each fix.
- Do NOT proceed to plugin packaging until all above are fixed.
- Reminder: do NOT edit ecomdrive plugin. Fashionary plugin only in draft/fashionary-woo-plugin/.
