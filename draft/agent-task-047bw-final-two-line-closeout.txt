P47bw - Final two-line closeout (must be 100% complete, no leftovers)

Goal
- Close the last 2 remaining gaps from P47bv.
- Make no unrelated changes.
- No commit/push instructions.

Scope (ONLY these two files)
- `src/app/dashboard/orders/client-page.tsx`
- `src/components/orders/order-details-view.tsx`

Out of scope
- No DB changes.
- No API changes.
- No refactor.

--------------------------------------------------------------------
A) Fix #1 - Mobile/card "View Details" must go through lock flow
--------------------------------------------------------------------

Current bad code (still present):
- File: `src/app/dashboard/orders/client-page.tsx`
- In mobile/card dropdown actions:
  - `<DropdownMenuItem asChild><Link href={`/dashboard/orders/${order.id}`}>View Details</Link></DropdownMenuItem>`

Required exact change:
- Replace that block with:
  - `<DropdownMenuItem onSelect={() => handleOpenOrder(order.id)}>View Details</DropdownMenuItem>`

Why:
- Direct `Link` bypasses lock check.
- `handleOpenOrder` enforces lock/override flow.

--------------------------------------------------------------------
B) Fix #2 - Order -> Issue (empty-state button) must use orderNumber first
--------------------------------------------------------------------

Current bad code (still present):
- File: `src/components/orders/order-details-view.tsx`
- In Issues tab empty-state "Create Issue" button:
  - `router.push(\`/dashboard/issues/new?orderId=${order.id}\`)`

Required exact change:
- Replace with:
  - `router.push(\`/dashboard/issues/new?orderId=${encodeURIComponent(order.orderNumber || order.id)}\`)`

Why:
- Requirement is human reference (`orderNumber`) first.
- Keep fallback to `order.id` only if orderNumber missing.

--------------------------------------------------------------------
C) Hard verification (mandatory, do not skip)
--------------------------------------------------------------------

1) Run these grep checks:

- Must return 0 matches:
  - `rg -n "DropdownMenuItem asChild><Link href=\\{`/dashboard/orders/\\$\\{order\\.id\\}`\\}>View Details</Link>" src/app/dashboard/orders/client-page.tsx`
  - `rg -n "router\\.push\\(`/dashboard/issues/new\\?orderId=\\$\\{order\\.id\\}`\\)" src/components/orders/order-details-view.tsx`

- Must return matches:
  - `rg -n "DropdownMenuItem onSelect=\\{\\(\\) => handleOpenOrder\\(order\\.id\\)\\}>View Details</DropdownMenuItem>" src/app/dashboard/orders/client-page.tsx`
  - `rg -n "router\\.push\\(`/dashboard/issues/new\\?orderId=\\$\\{encodeURIComponent\\(order\\.orderNumber \\|\\| order\\.id\\)\\}`\\)" src/components/orders/order-details-view.tsx`

2) Manual flow test:

- Lock test:
  1. User A opens one order.
  2. User B opens same order from mobile/card actions "View Details".
  3. Expected: lock warning/override flow appears (no direct open).

- Issue prefill test:
  1. Open order details.
  2. Go to Issues tab where no issues exist.
  3. Click "Create Issue".
  4. Expected: new issue page prefilled with order number (e.g., `150226-02`), not UUID.

3) Type safety:
- `npx tsc --noEmit --pretty false`
- Ignore unrelated pre-existing errors, but ensure no new error from the 2 edited lines.

Definition of Done
- Both bad patterns are removed.
- Both good patterns exist.
- Manual lock and prefill behaviors match expected results.

