UPDATE P03d - Remaining gaps after P03c verification

Scope: ONLY fix items listed here. No refactors. Keep changes minimal.

1) Product search combo needs cursor + Load More
File: src/app/dashboard/inventory/client-page.tsx
Component: ProductSearchCombo
Issue: Current combo only loads first 50 results and has no cursor/load-more.

Fix:
- Add state: `nextCursor`, `isLoadingMore`.
- On open/search change, reset list and cursor, then fetch first page.
- Add `loadMore()` that calls getProductsPaged({ search, pageSize: 50, cursor: nextCursor }) and appends items.
- Render a small "Load more" button at bottom of the CommandList when nextCursor exists.
- Ensure duplicates are avoided when appending (by id).

Expected behavior:
- Large product catalogs can be navigated beyond 50 results.

2) Purchases lot refresh should include locationId filter
File: src/app/dashboard/purchases/[id]/client-page.tsx
Function: refreshInventoryLots
Issue: query does not include locationId, so results are broader than needed.

Fix:
- Add locationId to params when available:
  - Prefer activeItem.locationId if present.
  - Else fallback to selectedLocationId (if set).
- Keep pageSize=50 and existing productId/variantId scoping.

Deliver back:
- Summary + files touched
- Manual verify:
  - Product search combo can Load More beyond 50.
  - Lot refresh requests include locationId in network query.
