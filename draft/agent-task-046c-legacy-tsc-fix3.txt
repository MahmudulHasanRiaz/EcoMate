UPDATE P46c - TSC cleanup (remaining errors, detailed single command)

Scope: Fix remaining errors in draft/tsc-legacy-errors-latest.txt only. Minimal changes.

1) Staff page Next types error (permissionActions export)
File: src/app/dashboard/staff/page.tsx
- Remove/relocate `permissionActions` export from this page module.
- If needed, move it to a separate helper file (e.g., src/app/dashboard/staff/permission-actions.ts) and import where used.
Reason: Next 15 disallows extra exports on page modules.

2) Customers list response typing
File: src/app/dashboard/customers/client-page.tsx
- Parse `page` safely:
  const page = Number(searchParams?.page ?? 1);
- Type API response:
  type CustomersApi = { items: Customer[]; nextCursor?: string | null; totalCustomers?: number; repeatCustomers?: number };
- Cast response as CustomersApi and use defaults:
  const nextCursor = data.nextCursor ?? null;
  const totalCustomers = data.totalCustomers ?? 0;
  const repeatCustomers = data.repeatCustomers ?? 0;

3) Expenses nullable check
File: src/app/dashboard/expenses/client-page.tsx
- Replace `expense.check` uses with `expense.check ?? 0`.

4) Inventory audit typing
File: src/app/dashboard/inventory/audit/client-page.tsx
- Convert nullable strings to undefined: `value={foo ?? undefined}` where type requires string | undefined.
- When constructing AuditItem, add `category: item.category ?? ''`.

5) Inventory main page missing helpers
File: src/app/dashboard/inventory/client-page.tsx
- Ensure `DialogTrigger` import exists from '@/components/ui/dialog'.
- Define `productById` map before use.
- Add `getVariantName` helper (fallback to attributes/sku).
- Guard dialog selected item: `if (!dialogState.selectedItem) return null` or `dialogState.selectedItem?.field`.

6) Orders page prop mismatch
File: src/app/dashboard/orders/[id]/page.tsx
- Replace `onSuccess` prop with `onOrderCreated` (or update component prop definition).

7) Orders client page missing handlers
File: src/app/dashboard/orders/client-page.tsx
- Define `handleInlineAssign` and `handleInlineStatus` OR remove JSX references.
  (Preferred: wire to existing update order service calls.)

8) Products page implicit any + function arg
Files:
- src/app/dashboard/products/client-page.tsx: type map param `(item: Product)`.
- src/app/dashboard/products/new/page.tsx: pass required arg or update called function to accept optional.

9) Remove snippet file
File: src/app/dashboard/purchases/[id]/client-page.tsx_snippet.ts
- Delete this file (legacy snippet causing errors).

10) Purchases new page typing
File: src/app/dashboard/purchases/new/page.tsx
- Ensure `unitCost` is provided on GeneralOrderItem objects.
- Remove or type-extend `printingCost` if not in ThreePieceOrderItem.
- Ensure `lotAllocations` present in draft type or cast appropriately.

11) Courier settings typing
File: src/app/dashboard/settings/courier/page.tsx
- Define a `CourierSettings` type matching fields (clientId, clientSecret, etc.) and cast fetched data.
- Ensure FieldDef.section uses only 'credentials' | 'webhook'.

12) Calendar typing
File: src/components/ui/calendar.tsx
- Cast components to any to allow IconLeft/IconRight:
  components={{ IconLeft: (props:any)=>..., IconRight: (props:any)=>... } as any

13) New order dialog items type
File: src/components/orders/new-order-dialog.tsx
- When building `items`, include `name` + `image` OR map to `products` field expected by OrderUpdateInput.

14) Staff pages status maps
Files:
- src/dashboard/staff/[id]/page.tsx
- src/app/dashboard/staff/[id]/page.tsx
- src/app/track-order/[query]/page.tsx
- src/app/dashboard/customers/[id]/page.tsx
Add `Draft` and `Damaged` keys to status label/color maps.

15) Staff page permission state typing
File: src/dashboard/staff/page.tsx and src/app/dashboard/staff/page.tsx
- Ensure permissions state matches expected shape: include `courierManagement`, `marketing`, `tasks`, `integrations` (with Permission or boolean).
- When passing permission to helper, avoid Record<string, boolean> (cast to Permission).

16) Re-run tsc
- Run: npx tsc --noEmit | Tee-Object -FilePath draft/tsc-legacy-errors-after.txt

Deliver back:
- Summary + files touched
- draft/tsc-legacy-errors-after.txt
