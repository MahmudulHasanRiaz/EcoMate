P47 Review – Latest claim (still NOT fully done)

What is OK now
- Phone normalization updated (normalizeBdPhone accepts 0XXXXXXXXXX and 880/+880). Good.
- IP is no longer printed in Order UI (“Network Protection” label only). Good.
- Scope selection added in Order + Customer pages. Good.
- /api/restrictions now supports scope/integration filters + uses custom message + forces IP to GLOBAL. Good.
- Post‑order restriction logic now uses findFirst/update/create (no invalid upsert). Good.
- woo-public.ts removed. Good.

Still broken / missing
1) Order page: restriction check uses stale `order` in useCallback
   - File: src/app/dashboard/orders/[id]/page.tsx
   - checkRestrictions() uses `order?.integrationId` but hook deps are []. This means SITE checks will be wrong (likely always false).
   - Fix: include integrationId in deps or pass integrationId as param to checkRestrictions.

2) Customer page: unblock deletes all scopes
   - File: src/app/dashboard/customers/[id]/page.tsx
   - Unblock uses DELETE only by targetHash → removes GLOBAL + SITE at once.
   - Fix: when unblocking, select exact target (SITE vs GLOBAL) and pass scope + integrationId to DELETE.

3) Plugin incomplete-orders request does NOT send X‑Forwarded‑For
   - File: draft/fashionary-woo-plugin/assets/js/checkout.js
   - Only PHP requests set X‑Forwarded‑For. JS capture does not.
   - Fix: add header in AJAX call or accept IP from server headers (if API is behind Cloudflare). At least remove claim “all API requests”.

4) Plugin phone validation still fragile with spaces/dashes
   - checkout.js uses regex on raw input; any formatting or spaces will fail.
   - Fix: normalize input by stripping non‑digits before regex test (same as normalizeBdPhone).

5) Customer page block scope uses mostRecentOrder
   - If customer has zero orders, SITE block sends integrationId null → will never match SITE checks.
   - Fix: disable SITE option when no order exists, or force GLOBAL in that case.

6) /api/restrictions GET used without integrationId in Order page
   - You do client filtering, but still OK. However, for SITE checks you should pass integrationId to reduce false positives.

Deliverables needed
- Fix items (1)–(5) at minimum.
- Provide evidence snippets and file list.
- Then we can proceed to plugin packaging.

Reminder
- No edits to ecomdrive.
- Fashionary plugin stays in draft/fashionary-woo-plugin/.
