UPDATE P01 - Cursor-only pagination (Core Lists)

Scope: ONLY convert listed list endpoints to cursor-only pagination. No refactors. Keep response shape stable. If a UI uses total/page, add an optional flag instead of breaking.

Targets
1) Orders list
- File: src/server/modules/orders.ts (getOrders)
  - Remove offset paging path (page/skip). Use cursor only.
  - Keep `nextCursor` and `hasMore`. Add optional `includeTotal` flag (default false) if you need counts.
- File: src/app/api/orders/route.ts
  - Accept `cursor` + `pageSize` + optional `includeTotal`.
- File: src/services/orders.ts
  - Stop sending `page` by default. Use cursor for next page.
- UI: src/app/dashboard/orders/client-page.tsx + src/app/dashboard/packing-orders/client-page.tsx
  - Use cursor-based pagination only.

2) Expenses list
- File: src/server/modules/expenses.ts (getExpenses)
  - Replace skip/offset with cursor pagination (order by date desc, id desc).
- File: src/app/api/expenses/route.ts
  - Accept cursor + pageSize; return nextCursor.
- File: src/services/expenses.ts + expenses UI
  - Switch to cursor. Keep pageSize cap (max 100 or 200).

3) Issues list
- File: src/server/modules/issues.ts (getIssues)
  - Replace skip/offset with cursor (orderBy createdAt desc + id desc).
- File: src/app/api/issues/route.ts
  - Accept cursor/pageSize, return nextCursor.
- File: src/services/issues.ts + issues UI
  - Switch to cursor. Keep filters.

4) Partners list (Suppliers/Vendors)
- File: src/services/partners.ts (getSuppliers, getVendors)
  - Replace page/offset with cursor (name asc + id asc) + nextCursor.
- File: src/app/api/partners/suppliers/route.ts + vendors/route.ts
  - Accept cursor/pageSize, return nextCursor.
- UI: partners pages
  - Use cursor. Keep search.

5) Product logs
- File: src/services/product-logs.ts
  - Convert offset page to cursor (timestamp desc + id desc).
- UI: src/app/dashboard/products/[id]/page.tsx
  - Use nextCursor for “load more”.

6) Courier return pending list
- File: src/server/modules/courier/reconciliation.ts (getReturnPendingOrdersPaginated)
  - Replace offset with cursor.
- File: src/app/api/courier/return-pending/route.ts
  - Accept cursor/pageSize.

Global rules
- Enforce server max pageSize (100/200).
- Return stable shape: { items, nextCursor, hasMore? }.
- `total` ONLY when includeTotal=true (default false).
- Update types in src/types if needed.

Deliver back
- Summary of changes + files touched
- Manual test: Orders list infinite scroll, Expenses list next page, Issues list next page, Partners list next page, Product logs load more.
