UPDATE P46b - Remaining TSC cleanup (single pass, detailed)

Scope: Fix all remaining TS errors from `npx tsc --noEmit` (see draft/tsc-legacy-errors-latest.txt). Keep changes minimal; prefer typing/guards over logic changes.

0) Re-capture baseline
- Run: npx tsc --noEmit | Tee-Object -FilePath draft/tsc-legacy-errors-latest.txt

1) Scripts: tighten types or no-check (safe)
- scripts/backfill-finance.ts
  Option A (preferred): add explicit types for `orders` and reduce callback params (sum/item).
  Option B (acceptable for one-off scripts): add `// @ts-nocheck` at top.
- scripts/discovery.ts
  Fix catch: `catch (e: unknown) { const err = e as Error; ... }`
- scripts/bullmq-worker.ts
  Fix null filter error around line ~125: avoid passing null to prisma filter. Use `const staffId = input.staffId ?? undefined;` and only include field if defined.

2) Products API + service reservedQuantity
- src/app/api/products/route.ts
  a) Type map callback param: `(item: any) => ...`
  b) Add `reservedQuantity: item.reservedQuantity ?? 0` in all Product mappings.
  c) If casting to Product, do `as Product` after adding missing fields.
- src/services/products.ts
  Add `reservedQuantity: item.reservedQuantity ?? 0` to mapProduct return (both list + detail).

3) Accounting label for staff payments
- src/server/modules/accounting.ts
  Add `salariesPayable: 'Salaries Payable'` to ACCOUNT_LABELS.
- src/app/api/staff/[id]/payments/route.ts
  Replace `ACCOUNT_LABELS.staffPayable` → `ACCOUNT_LABELS.salariesPayable`.

4) Staff API type predicate
- src/app/api/staff/route.ts
  Remove broken type predicate. Use:
  `const items = Array.isArray(res?.data) ? res.data : [];`
  Then map/filter `items` safely.

5) OrderStatus maps: add Draft + Damaged
Add missing keys to any `Record<OrderStatus,string>` maps in:
- src/app/dashboard/customers/[id]/page.tsx
- src/app/dashboard/staff/[id]/page.tsx
- src/dashboard/staff/[id]/page.tsx
- src/app/track-order/[query]/page.tsx
(Use same labels/colors as other status entries, e.g. Draft:'Draft', Damaged:'Damaged')

6) Webhook failures page typing
- src/app/dashboard/webhook-failures/page.tsx
  a) Replace `permissions?.integrations?.read` with a safe check (Permission can be boolean):
     `const canReadIntegrations = typeof permissions?.integrations === 'object' ? permissions.integrations.read : !!permissions?.integrations;`
  b) Button size: replace `size="xs"` with `size="sm"` (Button only supports default|sm|lg|icon).
- src/services/webhooks.ts (or type file)
  Add `status?: 'Open' | 'Resolved' | 'Ignored'` to `GetWebhookFailuresParams` so UI can pass status.

7) Calendar component typing
- src/components/ui/calendar.tsx
  Cast components to `any` or update types to allow IconLeft/IconRight.
  Minimal fix:
  `components={{ IconLeft: (props: any) => <ChevronLeft {...props} />, IconRight: (props: any) => <ChevronRight {...props} /> } as any`
  Also type `className` params: `(props: { className?: string }) => ...`

8) Chart component typing
- src/components/ui/chart.tsx
  Loosen props for tooltip/legend to avoid payload/label errors:
  - Type `props: any` for tooltip/legend renderers.
  - Any map over payload should guard `Array.isArray(payload)`.
  This resolves account/analytics chart errors.

9) Account page chart props
- src/app/dashboard/account/page.tsx
  Ensure chart components are imported from recharts or from ui/chart correctly.
  If still failing, cast chart props to `any`:
  `const pieData = ... as any;`

10) Accounting checks dialog
- src/app/dashboard/accounting/checks/_components/check-details-dialog.tsx
  a) Guard `payment.id`: `const paymentId = payment.id ?? ''` before use.
  b) If `payment.poId` not on type, use optional chaining or extend type in src/types to include `poId?: string | null`.

11) Courier pending order type
- src/app/dashboard/courier/client-page.tsx
  Use `order.business?.name ?? ''` instead of `order.businessName`, or extend ReturnPendingOrder type with `businessName?: string`.

12) Customers list typing
- src/app/dashboard/customers/client-page.tsx
  a) Type `page` param: `const page = Number(searchParams?.page ?? 1)` or cast to string.
  b) Type API response: define interface `{ items: Customer[]; nextCursor?: string; totalCustomers?: number; repeatCustomers?: number }` and use it.

13) Expenses client typing
- src/app/dashboard/expenses/client-page.tsx
  a) `account.name` errors: if account is string, use `account` directly; otherwise fix account type.
  b) `expense.check` possibly undefined: use `expense.check ?? 0`.
  c) Toast/Badge variant: replace "success" with "default" or "secondary".

14) Inventory audit typing
- src/app/dashboard/inventory/audit/client-page.tsx
  a) Replace `string | null` with `?? undefined` in inputs.
  b) When constructing AuditItem, add required `category: item.category ?? ''`.

15) Inventory main page missing symbols
- src/app/dashboard/inventory/client-page.tsx
  a) Import `DialogTrigger` from '@/components/ui/dialog'.
  b) Define `productById` map (e.g., `const productById = new Map(products.map(p=>[p.id,p]));`).
  c) Add `getVariantName` helper (use variant.name or attributes join).
  d) Guard `dialogState.selectedItem` before access.

16) Orders pages missing handlers
- src/app/dashboard/orders/[id]/page.tsx
  Replace `onSuccess` prop with `onOrderCreated` (or update component prop type).
- src/app/dashboard/orders/client-page.tsx
  Add `handleInlineAssign` and `handleInlineStatus` (call existing update APIs), or remove JSX references.

17) Products UI typing
- src/app/dashboard/products/client-page.tsx
  Type map callback param: `(item: Product) => ...`.
- src/app/dashboard/products/new/page.tsx
  Fix function call expecting argument; pass empty array or update signature to optional.

18) Purchases snippet file
- Delete `src/app/dashboard/purchases/[id]/client-page.tsx_snippet.ts` (legacy snippet causing errors).

19) Purchases new page type mismatches
- src/app/dashboard/purchases/new/page.tsx
  a) Ensure `unitCost` is supplied for `GeneralOrderItem` (use existing rate field).
  b) `printingCost` not in ThreePieceOrderItem: remove field or extend type.
  c) Ensure `lotAllocations` exists when casting to ThreePieceOrderItemDraft.

20) Courier settings typing
- src/app/dashboard/settings/courier/page.tsx
  Define a `CourierSettings` type and cast fetched settings to it; avoid `{}`.
  Ensure FieldDef.section is `"credentials" | "webhook"` or adjust values.

21) Staff page permissions typing
- src/app/dashboard/staff/page.tsx
  When passing permission values, ensure type is `Permission | boolean`, not `Record<string, boolean>`.
  Separate `pageAccess` from module permission list.

22) use-permissions forcedPermissions
- src/hooks/use-permissions.tsx
  Define `const forcedPermissions = null;` or remove references if unused.

23) Webhook failures params
- src/app/dashboard/webhook-failures/page.tsx
  Add `status` to params type (Step 6) and ensure API uses it.

24) Re-run TSC
- Run:
  npx tsc --noEmit | Tee-Object -FilePath draft/tsc-legacy-errors-after.txt
- If any errors remain, list them in reply.

Deliver back:
- Summary + files touched
- draft/tsc-legacy-errors-latest.txt + draft/tsc-legacy-errors-after.txt
- Note any unresolved errors
