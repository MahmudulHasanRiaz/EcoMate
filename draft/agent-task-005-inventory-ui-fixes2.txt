UPDATE P03f - Inventory product names/images + variant labels + actions menu

Scope: ONLY fix items listed here. Keep changes minimal.

1) Fix inventory item mapping (product/variant names + images)
File: src/server/modules/inventory.ts
Function: getInventoryPaginated

Problems:
- productName is currently set to variant name (shows "42" etc).
- variantName is not returned (UI shows "Variant" or attribute).
- images return invalid URLs -> 404.

Fix:
A) Product query: include image
  - In prisma.product.findMany select, add `image`.

B) Inventory item include: add variant.image (optional)
  - In prisma.inventoryItem.findMany include, add `image` in variant select.

C) Add helpers inside inventory.ts:
  - parseProductImage(imageString): string | undefined
    - If JSON array string, return first url.
    - Else if string, use as-is.
  - normalizeUrl(url): string | undefined
    - If empty -> undefined
    - If starts with http(s) -> return
    - If starts with '/' -> return
    - Else prefix '/'.

D) Build correct fields when creating map entry:
  - productName: product.name
  - variantName: row.variant?.name ?? undefined
  - productSku: product.sku
  - variantSku: row.variant?.sku ?? undefined
  - sku: row.variant?.sku || product.sku || 'N/A' (keep existing use)
  - productImage: normalizeUrl(parseProductImage(product.image))
  - variantImage: normalizeUrl(row.variant?.image) || productImage

2) InventoryItem type update
File: src/types/index.ts
Update InventoryItem:
  - add optional fields:
    productSku?: string;
    variantSku?: string;
    productImage?: string;
    variantImage?: string;

3) Client mapping + image usage
File: src/app/dashboard/inventory/client-page.tsx

A) productsFromItems map:
  - Use item.productName for name.
  - Use item.productSku for sku (fallback to item.sku).
  - Use item.productImage for image.
  - Variant list should use:
    name: item.variantName || item.variantSku || 'Variant'
    sku: item.variantSku || item.sku
    image: item.variantImage || item.productImage

B) getProductImage / getVariantImage:
  - Use productImage / variantImage from productsFromItems.
  - Fallback to DEFAULT_PLACEHOLDER if missing.

C) InventoryProductPickerDialog display line:
  - Replace the broken joiner "ƒ?›" with ASCII " - ".
  - Show "SKU: {item.variantSku || item.sku}" when variant exists, else productSku/sku.

4) Actions menu missing for variable products
File: src/app/dashboard/inventory/client-page.tsx
Function: findItemForVariant
Fix:
  - If no base item exists, return first item of the product group.
  - This enables View Movement/Lots/Details for variable products.

Deliver back:
- Summary + files touched
- Manual verify:
  - Inventory list shows correct product name (not "42").
  - Variant rows show variant name or variant SKU (not attribute-only).
  - Product images no longer 404 (or fall back to placeholder).
  - Three-dot actions work on variable products.
