UPDATE P04a - Fix missing chart-of-accounts import (Expenses build error)

Scope: ONLY fix the missing import. No refactors.

Problem:
Build error in `src/app/dashboard/expenses/client-page.tsx`:
`Module not found: Can't resolve '@/services/chart-of-accounts'`

Fix:
1) Update the import path
File: src/app/dashboard/expenses/client-page.tsx
Change:
```
import { getChartOfAccounts } from '@/services/chart-of-accounts';
```
to:
```
import { getChartOfAccounts } from '@/services/accounting';
```

2) Verification
Run:
```
rg -n "chart-of-accounts" src/app/dashboard/expenses/client-page.tsx
```
Expected: 0 matches.

Deliver back:
- Summary + file touched
- Paste the rg output
