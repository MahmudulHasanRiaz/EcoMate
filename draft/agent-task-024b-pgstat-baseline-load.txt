UPDATE P24b - Capture meaningful pg_stat_statements baseline (after real traffic)

Scope: DB-only. No code changes.
Goal: Get a useful slow-query baseline (not just CREATE EXTENSION).

Steps:
1) Reset stats (so only new queries appear):
   $env:PGPASSWORD = '123456'
   & "C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -d fashionary -h localhost -p 5432 -c "SELECT pg_stat_statements_reset();"

2) Generate real traffic (manual UI flows, 5–10 mins):
   - Open these pages once each (with filters/search):
     a) /dashboard/orders/all (search + filter + load more)
     b) /dashboard/purchases (list + open a PO)
     c) /dashboard/inventory (search + open View Stock + Movements)
     d) /dashboard/expenses (list + filter)
     e) /dashboard/check-passing (search + summary)
     f) /dashboard/analytics (load KPIs)
     g) /dashboard/marketing (overview + campaigns)
   - Optional: export 10 orders (async export)

3) Capture baseline:
   & "C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -d fashionary -h localhost -p 5432 -c "SELECT now() AS captured_at, calls, total_exec_time, mean_exec_time, rows, query FROM pg_stat_statements WHERE dbid = (SELECT oid FROM pg_database WHERE datname = current_database()) ORDER BY total_exec_time DESC LIMIT 30;" | Out-File -FilePath "draft\pg-stat-report.txt" -Encoding utf8

Deliver back:
- Updated draft/pg-stat-report.txt
- Note which pages were exercised
