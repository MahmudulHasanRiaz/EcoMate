UPDATE 9 - Fix save failure: Prisma client/schema out-of-sync (printingDamagedQty unknown)

Symptom:
- Saving Printing step throws Prisma error: Unknown argument `printingDamagedQty` in PurchaseOrderItem.updateMany.
- This indicates Prisma Client is generated without new fields, or migration not applied.

Actions (safe):
1) Verify migration exists and is applied
- Check `prisma/migrations/20260115195000_add_three_piece_fields` exists (it does).
- Run: `npx prisma migrate status`
- If pending, apply: `npx prisma migrate deploy`

2) Regenerate Prisma Client
- Run: `npx prisma generate`
- Restart dev server (stop/start). This clears old Prisma client loaded in memory.

3) If still failing (cache issue)
- Delete `.next` build cache and restart dev server.

No code changes needed unless schema is missing fields.
If schema is missing in some branch, add `printingDamagedQty`, `cuttingDamagedQty`, `finishingWastageQty` to PurchaseOrderItem (but main branch already has them).

Deliver back: result of migrate status + confirmation error gone.
