UPDATE P36c - Fix Webhook Failures access for Admin (UI gate + loading)

Scope: ONLY src/app/dashboard/webhook-failures/page.tsx and draft/task.txt. Minimal changes.

Problem:
- Admin user sees Access Denied on Webhook Failures page.
- Likely due to permissions not loaded yet or role mismatch; page shows Access Denied immediately.

Fix:
1) Add Clerk role guard + loading
File: src/app/dashboard/webhook-failures/page.tsx
- Import useUser:
  import { useUser } from '@clerk/nextjs';
- Inside component:
  const { user, isLoaded } = useUser();
  const role = user?.publicMetadata?.role as string | undefined;
  const isAdmin = role?.toLowerCase() === 'admin';
- Update hasAccess to:
  const hasAccess = Boolean(isAdmin || permissions?.integrations?.read);

- Add loading guard before Access Denied:
  if (!isLoaded || permissions === null) {
    return (
      <div className="flex h-[60vh] items-center justify-center">
        <Skeleton className="h-24 w-full max-w-md" />
      </div>
    );
  }

2) Ensure Access Denied only after load
- Keep existing Access Denied block, but now it triggers only after isLoaded && permissions !== null.

3) Update task tracker
File: draft/task.txt
- If P32/P34 were marked DONE-VERIFIED, move them back to DONE-UNVERIFIED until this fix is verified.
- Add note under P34: "Access Denied fix pending verification".

Deliver back:
- Summary + files touched
- Confirm Admin can access when role metadata is 'Admin'
- Note whether task.txt status adjusted
