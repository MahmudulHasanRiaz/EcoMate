UPDATE 20 - Fix malformed HTML tags reintroduced in invoice template

Scope: ONLY fix malformed tags in handleGenerateWorkOrder. No logic changes.

File: src/app/dashboard/purchases/[id]/client-page.tsx
Section: handleGenerateWorkOrder -> itemsHtml + metaRows

Problem:
Update 19 reintroduced invalid tags like `< div >` and `</div >` in multiple blocks.
This breaks parsing/build again.

Fix:
1) Replace all `< div >` with `<div>` and `</div >` with `</div>` within:
   - FABRIC itemsHtml
   - PRINTING itemsHtml
   - CUTTING itemsHtml
   - FINISHING itemsHtml
   - metaRows template strings

2) Confirm no malformed `< table >` or `</table >` remain.

Quick check after fix:
Run `rg "<\\s+div\\s*>|</\\s+div\\s*>" src/app/dashboard/purchases/[id]/client-page.tsx` -> should return no hits.

Deliver back: summary + confirmation build passes.
