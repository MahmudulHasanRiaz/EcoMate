UPDATE PXXc - Dual tracking (agent MD + project TXT)

Scope: Process only. No code changes.

Goal
- Keep your internal task.md / walkthrough.md for your own system.
- ALSO keep our project tracker in sync: draft/task.txt (single source for this repo).

Rules
1) Do NOT remove or downplay your internal MD files.
2) After each task, update draft/task.txt using our standard format:
   - ID, Title, Status, Evidence (file paths + command output + timestamp), Notes.
3) If your internal MD says "done", but no local evidence yet, mark as DONE-UNVERIFIED in draft/task.txt.
4) Replace any evidence pointing to non-existent files (e.g., task.md) with real repo evidence.
5) If build verification fails, add/keep a BUILD-TS entry in task.txt as BLOCKED with tsc output summary.

Deliver back
- Confirm both tracking systems updated:
  - Your internal MDs (no repo changes required).
  - draft/task.txt updated with real evidence.
