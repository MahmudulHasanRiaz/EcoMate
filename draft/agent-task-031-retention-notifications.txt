UPDATE P31 - Align data retention policy (Notifications cleanup removal)

Scope: Notifications API only. Keep changes minimal. No refactors.

Problem:
- Plan says "data retention indefinite".
- Current GET /api/notifications auto-deletes notifications older than 30 days. This violates retention policy.

Fix:
File: src/app/api/notifications/route.ts
1) Remove the auto-cleanup block:
```
// Auto-cleanup: remove notifications older than 30 days
const thirtyDaysAgo = new Date();
thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30);
prisma.notification.deleteMany({ ... }).catch(...)
```
2) Leave the rest of the handler unchanged.

Optional (if you want a toggle, skip for now):
- If you want cleanup in future, create a separate admin-only endpoint or cron job behind explicit settings. Do NOT add it now.

Deliver back:
- Summary + file touched
- Confirm no deleteMany in notifications GET

Notes:
- No schema changes.
- This strictly enforces indefinite retention.
