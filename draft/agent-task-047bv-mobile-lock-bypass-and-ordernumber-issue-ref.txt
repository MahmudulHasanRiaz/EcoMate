P47bv - Final mobile lock bypass fix + order-number-based issue reference

Goal
- Close the last remaining lock bypass in orders mobile/card actions.
- Ensure when creating an issue from an order, order **number** is used as reference (not raw internal order id).
- Keep implementation lightweight, no schema changes.
- No commit/push instructions.

Scope
- `src/app/dashboard/orders/client-page.tsx`
- `src/components/orders/order-details-view.tsx`
- `src/app/dashboard/issues/new/client-page.tsx`
- `src/app/dashboard/issues/client-page.tsx`
- `src/app/dashboard/issues/[id]/client-page.tsx`

Out of scope
- No changes in DB schema or migrations.
- No purchase/staff/accounting edits.

--------------------------------------------------------------------
1) CRITICAL: Fix mobile/card "View Details" lock bypass
--------------------------------------------------------------------

Problem
- Mobile/card dropdown still has direct `<Link>` for "View Details", bypassing lock acquire flow.

Required
1. In `src/app/dashboard/orders/client-page.tsx` mobile card dropdown actions:
   - Replace:
     - `<DropdownMenuItem asChild><Link href={...}>View Details</Link></DropdownMenuItem>`
   - With:
     - `<DropdownMenuItem onSelect={() => handleOpenOrder(order.id)}>View Details</DropdownMenuItem>`

Acceptance
- All "View Details" entry points (table + card/mobile) go through `handleOpenOrder`.
- If another user holds lock, override dialog appears (no direct open).

--------------------------------------------------------------------
2) Order -> Issue create must use `orderNumber` reference
--------------------------------------------------------------------

Business requirement
- When creating an issue from an order screen, use order number as reference string.
- Internal normalization to real order id stays server-side (already supported in `/api/issues`).

Required
1. In `src/components/orders/order-details-view.tsx` replace all issue-create navigations:
   - From `?orderId=${order.id}`
   - To `?orderId=${encodeURIComponent(order.orderNumber || order.id)}`

Expected occurrences:
- "Report Issue" button (desktop)
- "Report Issue" action (mobile dropdown)
- "Create Issue" button in empty issues state

Note
- Keep query parameter key as `orderId` for compatibility with existing API/service.
- Only change the value to prefer `order.orderNumber`.

Acceptance
- Opening "Create Issue" from order page prefills with order number (e.g., `150226-02`), not internal id.

--------------------------------------------------------------------
3) Issues UI wording + display polish for order reference
--------------------------------------------------------------------

Required
1. In `src/app/dashboard/issues/new/client-page.tsx`:
   - Change form label:
     - `Order ID (optional)` -> `Order Number / ID (optional)`
   - Update placeholder to reflect order number-first style.

2. In `src/app/dashboard/issues/client-page.tsx`:
   - Anywhere order text currently renders only `issue.orderId`, change to:
     - display label: `issue.orderNumber || issue.orderId`
   - Keep order detail link target by real id when available:
     - href should remain `/dashboard/orders/${issue.orderId}` when `issue.orderId` exists.
   - For card/table views, ensure shown text is order number when present.

3. In `src/app/dashboard/issues/[id]/client-page.tsx`:
   - In "For Order ..." description:
     - display text must be `issue.orderNumber || issue.orderId`
     - link target uses `issue.orderId` (internal route safety)

Acceptance
- Issue pages show order number as human reference whenever available.
- Navigation still works reliably via internal `issue.orderId`.

--------------------------------------------------------------------
4) Verification checklist (mandatory)
--------------------------------------------------------------------

A) Lock behavior
1. User A opens order details.
2. User B from mobile/card actions taps "View Details".
3. Must show lock conflict/override flow (not direct open).

B) Issue creation reference
1. Open an order with visible order number.
2. Click "Report Issue" from order details.
3. New issue page field must prefill with order number (not raw id).
4. Submit issue successfully.
5. In issues list/details, order reference should display order number.
6. Clicking order link from issue should still open correct order details.

C) Static check
1. Run:
   - `npx tsc --noEmit --pretty false`
2. Confirm no new TypeScript error introduced in above scoped files.

