UPDATE P13c - Expense approval flow final hardening (POST guard + payment field lock + platform enum)

Scope: ONLY expenses module + print view. Keep changes minimal.

Remaining issues:
1) POST /api/expenses allows isPaid=true even when status is Submitted.
2) Payment fields in ExpenseForm are still editable for non‑approved expenses.
3) Prepared By signature misses submitted date.
4) Expense platform options still include non‑enum values (Google, Pinterest, etc) which will break Prisma.

Fixes:

1) Enforce approval before payment on CREATE
File: src/app/api/expenses/route.ts
Add before createExpense:
```
const approvalStatus = (body?.approvalStatus as ExpenseApprovalStatus | undefined)
  ?? (body?.staffPaymentId ? 'Approved' : 'Submitted');
if (body?.isPaid === true && approvalStatus !== 'Approved') {
  return NextResponse.json({ error: 'Expense must be Approved before payment is recorded' }, { status: 400 });
}
```
Use approvalStatus variable when calling createExpense (so it matches the same decision).

2) Lock payment fields unless Approved (UI)
File: src/app/dashboard/expenses/expense-form.tsx
- Change isPaymentDisabled to:
  `const isPaymentDisabled = approvalStatus !== 'Approved';`
- Apply disabled={isPaymentDisabled} to:
  paidAt Input
  paidFromAccount SelectTrigger
  check amount Input
  checkNo Input
  checkDate Input
- Keep isPaid checkbox disabled={isPaymentDisabled} (already present).

3) Print Prepared By date
File: src/app/print/expenses/client-page.tsx
- In "Prepared By" block, add submitted date if available:
  `{expenses[0].submittedAt ? format(new Date(expenses[0].submittedAt), 'MMM d, yyyy') : ''}`

4) Align expense platform options to enum
File: src/app/dashboard/expenses/expense-form.tsx
- Update zod enum to match OrderPlatform:
  ['TikTok','Messenger','Facebook','Instagram','Website']
- Update Select options to the same list.
Reason: prevents Prisma enum error on save.
If you want Google/Other platforms later, we must extend OrderPlatform enum with a migration.

Deliver back:
- Summary + files touched
- Manual verify:
  - Creating paid expense without approval returns 400
  - Payment fields disabled until Approved
  - Prepared By shows name + date in print
  - Expense platform save works without Prisma enum errors
