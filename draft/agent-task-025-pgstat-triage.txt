UPDATE P25 - pg_stat_statements triage + index/action plan (analysis only)

Scope: Analysis + recommendations. No code changes.

Inputs:
- draft/pg-stat-report.txt (current baseline)

Goals:
1) Identify top 5-8 costly queries by total_exec_time or mean_exec_time.
2) Map each query to module/endpoint.
3) Check whether proper indexes already exist.
4) Propose concrete fixes: index, query change, or cache.
5) Produce a short actionable list for next implementation step.

Steps:
A) Read baseline:
- Open draft/pg-stat-report.txt
- List top 8 queries by total_exec_time.

B) For each query:
- Identify table(s) + where/order by/limit/offset.
- Locate the code path (server module/API/UI) that emits it.
- Verify index presence (schema.prisma + migrations):
  - use rg in prisma/schema.prisma for @@index or @index for those columns.
  - if missing, mark as "Index candidate".

C) Optional EXPLAIN (local DB):
- For top 3 read queries, run EXPLAIN (ANALYZE, BUFFERS) with realistic params (replace $1/$2) if feasible.
- Save outputs into draft/pg-stat-analysis.txt.

D) Deliverable doc:
- Create draft/pg-stat-analysis.txt with:
  1) Top queries list + impact
  2) Mapping to modules
  3) Index recommendations (exact columns)
  4) Query/logic changes if needed
  5) Next action shortlist (P25b) for implementation

E) Update task tracker:
- Add P24 entry in draft/task.txt under Verified Done with evidence:
  - File: draft/pg-stat-report.txt
  - Timestamp from report
  - Note that pg_stat_statements enabled (shared_preload_libraries)

Deliver back:
- draft/pg-stat-analysis.txt
- Updated draft/task.txt
- Summary of top recommendations
