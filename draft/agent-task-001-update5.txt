UPDATE 5 - Add Cutting Master role (invite + permissions + page access)

Scope: only add the role. No refactors.

Decision baseline (per client):
- Role label in UI: "Cutting Master" (maps to DB enum CuttingMan).
- Page access: only Dashboard + Account (account is always accessible; no menu access).
- Permissions: read-only ONLY for Orders so dashboard stats do not error; everything else no-access.
  If you need a different module list, pause and ask before proceeding.

1) Role dropdowns (UI)
File: src/app/dashboard/staff/page.tsx
- Add 'Cutting Master' to staffRoles list (used for invite + filters).

2) Role mapping (API)
Files:
- src/app/api/staff/route.ts
- src/app/api/staff/[id]/route.ts
- src/app/api/staff/invite/route.ts

Changes:
- Add UI -> DB mapping: 'Cutting Master' => 'CuttingMan'.
- Add DB -> UI mapping: 'CuttingMan' => 'Cutting Master'.
- Ensure invites persist role as CuttingMan.

3) Preset permissions
File: src/lib/staff-permissions.ts
- Add preset for 'Cutting Master' with:
  orders: readOnly
  all other modules: noAccess

4) Page access preset
File: src/lib/page-access.ts
- Add ROLE_PAGE_ACCESS entry for 'Cutting Master': ['pages.dashboard']
  (Account page is not gated by page access, so it remains visible.)

5) Verify role appears in staff filter
- No separate change if staffRoles array is reused in filters.

Deliver back: summary + tests run (if any).
