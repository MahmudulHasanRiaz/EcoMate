P47bn-delta - Recheck fixes for general receive wastage + category tree UX (must close remaining gaps)

Scope:
- Close remaining gaps from P47bn implementation.
- Keep existing behavior that already works.
- No unrelated refactor.
- No commit/push instructions.

Files to touch:
- prisma/migrations/20260215164000_add_general_wastage_qty/migration.sql
- src/services/purchases.ts
- src/server/modules/purchases.ts
- src/app/dashboard/purchases/[id]/client-page.tsx
- src/components/products/category-tree-select.tsx

--------------------------------------------------------------------
1) Migration hardening (production-safe)
--------------------------------------------------------------------

Problem:
- Migration exists, but plain ADD COLUMN can fail on environments where column was already introduced manually.

Do:
- Update `prisma/migrations/20260215164000_add_general_wastage_qty/migration.sql` to be idempotent.
- Use a guarded statement, e.g.:
  ALTER TABLE "PurchaseOrderItem" ADD COLUMN IF NOT EXISTS "generalWastageQty" INTEGER NOT NULL DEFAULT 0;

--------------------------------------------------------------------
2) Expose `generalWastageQty` in all purchase mappings
--------------------------------------------------------------------

Problem:
- UI reads `item.generalWastageQty`, but mapping does not always include it.

Do:
A) In `src/services/purchases.ts` inside purchaseItems mapper, add:
- `generalWastageQty: (item as any).generalWastageQty ?? 0`

B) In `src/server/modules/purchases.ts` inside `mapDbPoToAppPo` purchaseItems mapper, add both:
- `receivedQty: (item as any).receivedQty ?? 0`
- `generalWastageQty: (item as any).generalWastageQty ?? 0`

Result:
- Remaining/coverage/wastage counters become consistent in all screens and refresh paths.

--------------------------------------------------------------------
3) Strict server validation for receive payload
--------------------------------------------------------------------

Problem:
- Current logic clamps negatives with `Math.max(0, ...)` instead of rejecting invalid input.

Do in `receivePurchaseOrderStockCore` (`src/server/modules/purchases.ts`):
A) Read raw values first:
- `rawReceive = Number(item.quantity)`
- `rawWastage = Number(item.wastageQty ?? 0)`

B) Reject invalid values with explicit errors:
- non-finite
- negative

C) Keep overflow validation:
- `rawReceive + rawWastage <= remainingForItem`

D) Use sanitized values only after validation.

--------------------------------------------------------------------
4) Guard zero-good-unit completion on wastage scenario
--------------------------------------------------------------------

Problem:
- On full completion with wastage, reallocation silently skips when baseGoodValue <= 0.

Do in `receivePurchaseOrderStockCore`:
- In reallocation block, if `isCompleted` and wastage exists and `totalBaseGoodValue <= 0`, throw a clear error:
  - Example: "Cannot finalize cost reallocation: no received good quantity."

Reason:
- Prevent silent completion with broken valuation.

--------------------------------------------------------------------
5) Receive scan remaining bug fix
--------------------------------------------------------------------

Problem:
- Scan +1 handler computes remaining using only `quantity - receivedQty`, ignoring cumulative wastage.

Do in `src/app/dashboard/purchases/[id]/client-page.tsx`:
- Update scan remaining formula to:
  `quantity - receivedQty - generalWastageQty`

--------------------------------------------------------------------
6) Add missing UX action: "Mark Remaining as Wastage"
--------------------------------------------------------------------

Problem:
- `Fill Remaining` exists, but missing quick action for wastage.

Do in `src/app/dashboard/purchases/[id]/client-page.tsx`:
- Add button beside `Fill Remaining`:
  - label: `Mark Remaining as Wastage`
  - behavior:
    - sets each row `itemWastageQtys[item.id] = remaining`
    - sets `itemReceiveQtys[item.id] = 0`

--------------------------------------------------------------------
7) Category tree selector final UX corrections
--------------------------------------------------------------------

File: `src/components/products/category-tree-select.tsx`

Required fixes:
A) Single-expand in normal mode:
- Use `Accordion type="single" collapsible` when searchTerm is empty.

B) Search mode behavior:
- In search mode, show filtered results without forcing multi-open accordion behavior.
- Keep selection working for parent and child.

C) Selected label context:
- Button label should show:
  - parent selection: `Parent`
  - child selection: `Parent > Child`

D) Cleanup:
- Remove unused imports (Command, Badge, etc.) if not used.

--------------------------------------------------------------------
8) Verification checklist (must run locally)
--------------------------------------------------------------------

A) Wastage mapping
1. Open purchase detail with existing general PO.
2. Confirm line shows both cumulative Received and Wasted values.
3. Refresh page after receive action; values remain correct.

B) Validation
1. Try negative receive/wastage payload from UI -> blocked (and server rejects if bypassed).
2. Try receive+wastage > remaining -> blocked.

C) Scan
1. If cumulative wastage already exists, scan +1 should still respect true remaining.

D) Reallocation guard
1. Try edge case with covered-by-wastage and zero good received.
2. Confirm explicit error instead of silent success.

E) Category selector
1. Normal mode: only one parent expanded at a time.
2. Search mode: filtered matches selectable.
3. Child selected label renders as `Parent > Child`.

Quick checks:
- `npx prisma validate`
- `npx tsc --noEmit`
