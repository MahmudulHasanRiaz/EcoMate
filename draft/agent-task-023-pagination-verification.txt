UPDATE P23 - Cursor pagination verification + fixes + evidence

Scope: Verify P01 series (orders, expenses, issues, partners, product logs, courier return pending). Fix only if gaps found. Update draft/task.txt with evidence.

Goal:
- Ensure cursor-only pagination (no offset/page).
- Ensure includeTotal gating for heavy aggregates.
- Ensure deterministic orderBy (date + id) with cursor.
- Ensure services/UI pass cursor + handle nextCursor.

Modules to verify (files):
1) Orders
- Server: src/server/modules/orders.ts (getOrders)
- API: src/app/api/orders/route.ts
- Service: src/services/orders.ts
- UI: src/app/dashboard/orders/client-page.tsx + packing-orders
Checks:
- No page/offset use.
- orderBy includes date/createdAt + id.
- includeTotal optional and only computed when requested.
- nextCursor returned + used in UI.

2) Expenses
- Server: src/server/modules/expenses.ts (getExpenses)
- API: src/app/api/expenses/route.ts
- Service/UI: src/services/expenses.ts + expenses list page
Checks:
- includeTotal gate around groupBy/summary.
- cursor param respected; nextCursor returned.

3) Issues
- Server: src/server/modules/issues.ts
- API: src/app/api/issues/route.ts
- Service/UI: src/services/issues.ts + issues page
Checks:
- cursor-only; filters preserved; includeTotal optional.

4) Partners
- API: src/app/api/partners/suppliers/route.ts and vendors/route.ts
- Server: src/server/modules/partners.ts (getSuppliers/getVendors)
- UI: partners list page uses cursor
Checks:
- cursor-only, minimal select, nextCursor in response.

5) Product Logs
- Server: src/server/modules/products.ts (getProductLogs?)
- API: src/app/api/products/[id]/logs/route.ts
- UI: product history uses cursor + Load More

6) Courier Return Pending
- Server: src/server/modules/courier.ts (getReturnPendingOrdersPaginated)
- API: src/app/api/courier/return-pending/route.ts
- UI: courier management page uses cursor

Deliverables:
A) If any gaps found, fix with minimal changes (cursor, includeTotal gating).
B) Update draft/task.txt: move each verified item from “Done (Unverified)” to “Verified Done” with evidence lines (file path + rg/findstr output + date).
C) Report any remaining gaps that require product decision.

Commands (evidence examples):
- rg -n "cursor" src/app/api/orders/route.ts
- rg -n "includeTotal" src/server/modules/expenses.ts
- rg -n "nextCursor" src/services/orders.ts

Return to me:
- Summary of fixes (if any)
- Evidence snippets for each P01 item
- Updated draft/task.txt
