# P47 review response (for coder)

Status: PARTIAL PASS

Verified OK
- Order Details restriction check passes integrationId (scope-aware).
- Customer Details: SITE scope disabled if no orders; unblock picks site/global.
- Restrictions API: GET includes GLOBAL when integrationId is provided.
- Plugin: phone normalization strips spaces/dashes before regex.
- PHP calls send X-Forwarded-For header.

Must fix (blocking)
1) Incomplete checkout SKU list is still placeholder and always empty.
   File: draft/fashionary-woo-plugin/assets/js/checkout.js
   - getCartSkus() returns [] => SKU list never sent.
   - This breaks dedupe/reporting that relies on cart items.

Required fix
A) Pass cart SKUs from Woo to JS, then use them in captureIncomplete.
   Option 1 (recommended): localize cart items from PHP.
   - In fashionary-woo-plugin.php, enqueue checkout.js and add:
     wp_localize_script('fashionary-checkout', 'FASHIONARY_CART', [
       'skus' => array_values(array_filter(array_map(function($item){
         return $item['data']? $item['data']->get_sku() : null;
       }, WC()->cart->get_cart())))
     ]);
   - In checkout.js:
     function getCartSkus(){
       if (window.FASHIONARY_CART && Array.isArray(window.FASHIONARY_CART.skus)) {
         return window.FASHIONARY_CART.skus;
       }
       return [];
     }

B) Update the report/claim notes:
   - Do NOT claim "all API requests send X-Forwarded-For" from JS.
     JS requests are client-side; IP is derived server-side.
     Leave PHP header as-is.

Non-blocking note (optional)
- If you want, pass integrationId in customer page GET when checking restrictions.
  Not required because API returns all matches without integrationId, but it can
  reduce ambiguity.

Reminder
- Do NOT edit the ecomdrive plugin. Only Fashionary plugin inside:
  draft/fashionary-woo-plugin/

After fix
- Re-run quick check:
  1) Place a checkout attempt with invalid phone -> blocked (red warning + no order).
  2) Incomplete capture payload should include skuList with real SKUs.
  3) No change needed for PHP restriction check; should still block with wc_add_notice.

Deliver back
- Brief diff summary
- Mention file(s) changed
- Evidence snippet from checkout.js showing getCartSkus implementation
