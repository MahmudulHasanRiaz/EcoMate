UPDATE P03c - Provide verification artifacts in /draft (no code changes)

Scope: ONLY create the artifact files listed below under /draft. Do NOT modify code.

Goal
You stated P03 fixes are complete, but your walkthrough/task files are not in this repo.
Provide your verification evidence as plain .txt files so it can be reviewed here.

Required outputs (all ASCII, no markdown):

1) draft/inventory-performance-walkthrough.txt
   - Copy the content you would put in your system walkthrough.md.
   - Must include:
     - Summary (what changed)
     - Files touched (full paths)
     - Key diffs (brief, per file)
     - Verification steps (exact commands + short outputs)

2) draft/inventory-performance-task.txt
   - Copy the P03-related entries you would put in task.md.
   - Each entry must include:
     - Status (Verified / Done-Unverified / Blocked)
     - Evidence: file paths + command output snippets

3) draft/inventory-performance-change-list.txt
   - One line per file changed: "path - short reason"

4) draft/inventory-performance-verification-log.txt
   - Commands you ran (exact) and trimmed outputs.
   - Include at least:
     - rg/findstr proof for inventory stats + pagination changes
     - tsc --noEmit (or note if not run)

Rules
 - Do not delete or edit any existing files.
 - Do not change code in this step.
 - If any item is not actually done, say "BLOCKED" and why in the walkthrough + task.

Deliver back
 - Confirm the 4 files are created with correct content.
