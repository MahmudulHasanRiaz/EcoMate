P47cf - Final TS blocker closeout with strict no-scope-creep guard

Goal
- Close all remaining `npx tsc --noEmit` blockers reported in latest run.
- Do NOT introduce any unrelated changes.
- Do NOT modify marketing/product logic in this pass.

Hard Rule (must)
- Only touch these files:
  1) `scripts/check-data.ts`
  2) `scripts/get-integration.ts`
  3) `src/app/api/orders/incomplete/[id]/convert/route.ts`
  4) `src/app/dashboard/partners/payment/[id]/invoice/page.tsx`
  5) `src/app/dashboard/staff/[id]/page.tsx`
  6) `src/app/track-order/[query]/page.tsx`
  7) `src/server/modules/orders.ts`
  8) `src/types/index.ts`

If any other file must be touched, STOP and report first.

--------------------------------------------------------------------
0) Pre-change scope snapshot (mandatory)
--------------------------------------------------------------------

Run:
```powershell
mkdir -Force .tmp | Out-Null
git diff --name-only | Set-Content .tmp/p47cf_before_changed_files.txt
```

--------------------------------------------------------------------
1) Fix remaining TS blockers exactly
--------------------------------------------------------------------

1.1 Prisma model access in scripts
File: `scripts/check-data.ts`
- Replace:
  - `prisma.wooIntegration.findMany(...)`
- With:
  - `prisma.wooCommerceIntegration.findMany(...)`

File: `scripts/get-integration.ts`
- Replace:
  - `prisma.wooIntegration.findFirst(...)`
- With:
  - `prisma.wooCommerceIntegration.findFirst(...)`

1.2 Staff auth union narrowing
File: `src/app/api/orders/incomplete/[id]/convert/route.ts`
- Current issue: unsafe `auth.staff` access before checking union status.
- Implement:
  - after `const auth = await getStaffAuthDetails();`
  - guard:
    - if `auth.status !== 'ok'` -> return unauthorized
    - then `const actor = auth.staff`

1.3 Partner invoice vendor address typing
File: `src/app/dashboard/partners/payment/[id]/invoice/page.tsx`
- `Vendor` schema has no `address`; do not access `payment.Vendor?.address`.
- Update partner address expression to a valid source only:
  - use `payment.PurchaseOrder?.Supplier?.address` (and safe fallback)
- Keep phone/email logic valid.

1.4 Staff page date + username issues
File: `src/app/dashboard/staff/[id]/page.tsx`
- Remove `instanceof Date` checks on union/primitive values that trigger TS2358.
- Use robust conversion:
  - `const d = new Date(value as any);`
  - `isNaN(d.getTime()) ? 'N/A' : ...`
- Replace non-existent `staffMember.username` usage with a valid field:
  - use `staffMember.email` (or `staffCode`), but no unknown property.

1.5 Track-order status color map strictness
File: `src/app/track-order/[query]/page.tsx`
- Change status color map type from strict `Record<Order['status'], string>` to:
  - `Partial<Record<Order['status'], string>>`
- Keep fallback when reading status color to avoid undefined style breaks.

1.6 Order include relation key
File: `src/server/modules/orders.ts`
- In `updateOrderStatusDirect` include block:
  - replace invalid `logs: true`
  - with valid relation include key used by Prisma model (`OrderLog: true`)
- Keep existing normalized return adapter behavior unchanged.

1.7 Placeholder-data status breakdown typing mismatch
File: `src/types/index.ts`
- In `StaffMemberUI.performance.statusBreakdown`, replace:
  - `Record<OrderStatus, number>`
- with:
  - `Partial<Record<OrderStatus, number>>`
- This avoids forcing both legacy + canonical status keys simultaneously.
- Do NOT broaden other financial or permission types.

--------------------------------------------------------------------
2) No-scope-creep verification (mandatory)
--------------------------------------------------------------------

2.1 Ensure only intended files changed in this pass
```powershell
$before = Get-Content .tmp/p47cf_before_changed_files.txt
$after = git diff --name-only
$newFilesTouched = $after | Where-Object { $_ -notin $before }
if ($newFilesTouched) {
  Write-Host "Unexpected newly touched files:"
  $newFilesTouched | ForEach-Object { Write-Host " - $_" }
  exit 1
}
```

2.2 TypeScript check
```powershell
npx tsc --noEmit --pretty false
```
- Must pass, or list only truly pre-existing blockers not from this scope.

2.3 Focused grep sanity
```powershell
rg -n "wooIntegration" scripts/check-data.ts scripts/get-integration.ts
rg -n "logs:\\s*true" src/server/modules/orders.ts
rg -n "username" src/app/dashboard/staff/[id]/page.tsx
```
- Expected:
  - no `wooIntegration` in those two scripts
  - no `logs: true` in `orders.ts`
  - no invalid `staffMember.username` use

--------------------------------------------------------------------
3) Output format (required)
--------------------------------------------------------------------

Reply with:
1) Exact files modified (from the whitelist)
2) `tsc` result
3) Confirmation that no unexpected new files were touched
4) Short note if any blocker remains and why

No extra feature work in this task.
