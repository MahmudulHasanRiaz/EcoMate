UPDATE P11b - Fix Check Passing denorm gaps (imports + staff sync + summary)

Scope: ONLY items listed here. Keep changes minimal. No refactors.

1) Fix build error in check-passing-items.ts (missing PrismaClient/DefaultArgs)
File: src/server/modules/check-passing-items.ts
- Current type uses PrismaClient + DefaultArgs but these are not imported.
- Simplest fix:
  - Update import line to include PrismaClient.
  - Replace TransactionClient type with Prisma.TransactionClient (no DefaultArgs needed).
Example:
```
import { Prisma, PrismaClient, CheckPassingSource, CheckStatus } from '@prisma/client';
// remove: import { Payment } from '@/types';

type TransactionClient = Prisma.TransactionClient;
```

2) StaffPayment sync hooks (CheckPassingItem)
Goal: whenever staff payment with check is created/updated, CheckPassingItem stays in sync.
Where:
- Find the POST/PUT path that creates/updates StaffPayment
  (likely src/app/api/staff/[id]/payments/route.ts or a server module used by it).
Implement inside the same transaction:
- After create/update:
  - if check > 0 AND checkDate exists:
      const item = await buildCheckPassingItemFromStaffPayment(tx, payment.id);
      if (item) await upsertCheckPassingItem(tx, item);
  - else:
      await deleteCheckPassingItem(tx, CheckPassingSource.Staff, payment.id);
Notes:
- Do NOT add new routes. Only patch the existing staff-payment create/update flow.
- Ensure this runs for both newly created and edited payments.

3) Check Passing summary should use CheckPassingItem table
File: src/app/api/check-passing/summary/route.ts
Replace the UNION query (PurchasePayment + Expense + StaffPayment) with CheckPassingItem:
SQL example:
```
SELECT "passingDate"::date AS day,
       COUNT(*)::int AS count,
       COALESCE(SUM("amount"), 0) AS total
FROM "CheckPassingItem"
WHERE "passingDate" >= DATE(${today})
  AND "passingDate" <= DATE(${end})
  AND "status" = 'Pending'
GROUP BY day
ORDER BY day ASC
```
Return the same response shape as now.

Deliver back:
- Summary of changes + files touched
- Note the staff-payment create/update location you patched
