UPDATE P12-2e - Marketing UI business filter cleanup (real list + correct ALL handling)

Scope: ONLY src/app/dashboard/marketing/page.tsx. Keep changes minimal.

Problem:
- Business filter has a hardcoded "BUS-1" and comment block.
- "All Businesses" uses value "ALL" which gets sent to API as a real businessId.
- This can return empty data and is not clean UI.

Fix:
1) Load real businesses
   - Import getBusinesses from '@/services/partners'.
   - Add state: const [businesses, setBusinesses] = useState<Business[]>([]);
   - On load (same place as loadMarketers), call getBusinesses() and set list.
   - Handle errors with console.error only (no toast needed).

2) Correct ALL handling
   - Use selectedBusinessId with value "all" for the All option.
   - Add helper:
     const effectiveBusinessId = selectedBusinessId === 'all' ? undefined : selectedBusinessId;
   - Use effectiveBusinessId in getMarketingOverview/getCampaigns calls.

3) Clean UI
   - Remove the long JSX comment block.
   - Replace hardcoded BUS-1 with real businesses:
     {businesses.map(b => (
        <SelectItem key={b.id} value={b.id}>{b.name}</SelectItem>
      ))}
   - Keep the All option:
     <SelectItem value="all">All Businesses</SelectItem>

Deliver back:
- Summary + file touched
- Confirm business filter no longer sends "ALL" to API
- Manual verify:
  - Switching business updates KPIs + campaigns
  - All Businesses shows combined view
