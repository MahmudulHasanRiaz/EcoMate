UPDATE P12-2b - Marketing module fixes (migration + auth + data wiring)

Scope: ONLY fix items listed. Keep changes minimal. No refactors.

1) Create proper migration for Marketing tables
Reason: db push used; no migration exists.
Run:
```
npx prisma migrate dev -n add_marketing_module
npx prisma generate
```
If drift errors appear (because db push already applied), do:
```
npx prisma migrate dev -n add_marketing_module --create-only
npx prisma migrate deploy
npx prisma generate
```
Deliver migration folder name.

2) Fix Prisma import (default export)
Files:
- src/server/modules/marketing.ts
- src/app/api/marketing/campaigns/[id]/route.ts
Change:
```
import { prisma } from '@/lib/prisma';
```
to:
```
import prisma from '@/lib/prisma';
```

3) Fix permission checks in all marketing API routes
Files:
- src/app/api/marketing/overview/route.ts
- src/app/api/marketing/campaigns/route.ts
- src/app/api/marketing/campaigns/[id]/route.ts
- src/app/api/marketing/campaigns/[id]/spend/route.ts
- src/app/api/marketing/campaigns/[id]/orders/route.ts
Replace:
```
const auth = await enforcePermission(req, 'marketing', 'read');
if (!auth) return NextResponse.json({ error: 'Unauthorized' }, { status: 403 });
```
with:
```
const { allowed, error, staff } = await enforcePermission('marketing', 'read');
if (!allowed) return error;
```
Use action 'create' or 'update' accordingly.

4) Wire createdById + marketerId correctly
Files:
- src/app/api/marketing/campaigns/[id]/spend/route.ts
- src/app/api/marketing/campaigns/[id]/orders/route.ts
- src/server/modules/marketing.ts

Rules:
- createdById should use staff.id (not auth.user).
- Attribution marketerId should default to campaign.marketerId if set.
- businessId should default to campaign.businessId.

Implementation:
a) In spend route:
```
const { allowed, error, staff } = await enforcePermission('marketing', 'update');
if (!allowed) return error;
const createdById = staff?.id || null;
```
Pass createdById to addSpend.
Update addSpend signature to allow createdById?: string | null (optional).

b) In orders route:
Use staff?.id if campaign.marketerId is null (fallback).
Enforce single-campaign attribution:
  - Before create, check if any orderId already attributed to a different campaign.
  - If found, return 400 with message "Order already attributed to another campaign."
Create many with skipDuplicates to avoid duplicates.

c) In addAttributions (server module):
- Fetch campaign by id (select marketerId, businessId).
- Use campaign.marketerId ?? fallback staffId.
- Use campaign.businessId if available.

5) Use OrderStatus enums (avoid raw strings)
File: src/server/modules/marketing.ts
- Import OrderStatus from '@prisma/client'
- Replace status filter strings with:
  [OrderStatus.Canceled, OrderStatus.Incomplete, OrderStatus.IncompleteCancelled]
Apply in getMarketingOverview + getCampaigns + getCampaignDetails.

6) Campaign creation should allow selecting marketer
File: src/app/dashboard/marketing/page.tsx
Add Marketer select in Create Campaign modal:
- Load marketers list via getStaff({ role: 'Marketer', pageSize: 200 })
- Add Select field "Marketer" (optional)
- If current user is Marketer, prefill to self
- Pass marketerId in createCampaign payload

7) Remove unused imports
File: src/server/modules/marketing.ts
- Remove unused unstable_cache if not used.

Deliver back:
- Summary + files touched
- Migration name
- Manual verify:
  - Create campaign with marketer set
  - Add spend (createdByName shows)
  - Attribute order; if already attributed elsewhere, error shows
  - Overview + marketer table show correct totals
