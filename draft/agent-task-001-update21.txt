UPDATE 21 - Invoice print image preload + lot rows clearer + lot remaining qty

Scope: ONLY items listed here. Keep changes minimal.

1) Print waits for all images (logo + product) before printing
File: src/app/dashboard/purchases/[id]/client-page.tsx
Section: handleGenerateWorkOrder -> after w.document.write/html and before printing

Fix:
- Replace current logo-only print wait with a full image preload:
  const images = Array.from(w.document.images);
  const waitForImages = () => Promise.all(images.map(img => img.complete ? Promise.resolve() : new Promise(res => {
      img.addEventListener('load', () => res(null), { once: true });
      img.addEventListener('error', () => res(null), { once: true });
  })));
- Add a timeout fallback (e.g. 1500–2000ms) so print never hangs.
- After wait/timeout, call print.

Goal: print layout always includes images on first click.

2) Lots layout: move into a full-width sub-row per product
File: src/app/dashboard/purchases/[id]/client-page.tsx
Section: handleGenerateWorkOrder -> itemsHtml (all steps)

Fix:
- Keep lotsHtml generation as a block, but render it in its own <tr> after each product row:
  <tr>
    <td colspan="6" style="padding:4px 8px;">
      ${lotsHtml}
    </td>
  </tr>
- Only insert this row when lotsHtml is non-empty.
- This makes the lots section clear and not cramped inside product cell.

3) Lots block content: include Remaining qty per lot
File: src/app/dashboard/purchases/[id]/client-page.tsx
Section: formatLots

Fix:
- Add a "Rem" column (remaining) using inventoryLotMap quantity:
  remaining = (lot?.quantity ?? 0)
- Show it alongside J/O/S values.
- Keep compact grid layout.

4) Optional (if helpful): show total per-part in lots block header row
- Add a "Total" row at top with J/O/S totals for that item
  (use it.jamaYards/ornaYards/selowarYards, formatted).
- This helps quickly compare allocations vs totals.
- Only if it doesn't clutter; otherwise skip.

Deliver back: summary + verify
- Printing invoice prints with images on first click.
- Lots show in a full-width sub-row with J/O/S + Rem, readable at a glance.
