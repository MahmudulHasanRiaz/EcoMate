UPDATE P12-1 - Add Marketer + Finance Manager roles + Marketing menu stub

Scope: ONLY roles + permissions + menu + placeholder page. No marketing data yet.

1) Prisma role enum
File: prisma/schema.prisma
- Add enum values:
  - Marketer
  - FinanceManager
- Create migration (no data loss).

2) Types + validation
Files:
- src/types/index.ts (StaffRole union + permissions)
- src/lib/validations/staff.ts
Add roles:
  UI labels:
    - "Marketer"
    - "Finance Manager"
DB values:
  - Marketer
  - FinanceManager
Ensure validation accepts both UI labels and DB enum values.

3) Role mapping (UI <-> DB)
Files:
- src/app/api/staff/route.ts
- src/app/api/staff/[id]/route.ts
- src/app/api/staff/invite/route.ts
- src/services/staff.ts
Add mapping:
  UI "Marketer" -> DB "Marketer"
  UI "Finance Manager" -> DB "FinanceManager"
  DB "Marketer" -> UI "Marketer"
  DB "FinanceManager" -> UI "Finance Manager"

4) Permissions + page access
Files:
- src/lib/staff-permissions.ts
- src/lib/page-access.ts
- src/types/index.ts (permissions object)
Add new module key: marketing
Set presets:
  Marketer:
    marketing: create/read/update
    analytics: readOnly
    orders: readOnly
    expenses: readOnly
    customers: readOnly (optional)
    rest: noAccess
  Finance Manager:
    expenses: full
    accounting: full
    checkPassing: full
    staff: readOnly
    purchases: readOnly
    analytics: readOnly
    marketing: readOnly
    rest: noAccess
Add page access key: pages.marketing (route /dashboard/marketing)
Role page access:
  Marketer -> dashboard + marketing + analytics + orders (+ customers if enabled)
  Finance Manager -> dashboard + expenses + accounting + checkPassing + staff + purchases + analytics + marketing

5) Role dropdown in staff UI
File: src/app/dashboard/staff/page.tsx
- Add "Marketer" and "Finance Manager" to staffRoles list.

6) Marketing menu + placeholder page
Files:
- Sidebar/nav component (search for analytics menu in src/components)
- src/app/dashboard/marketing/page.tsx
Add menu item "Marketing" (icon optional) pointing to /dashboard/marketing.
Render a simple placeholder page with header "Marketing" and subtitle "Reports coming soon".
Gate menu by marketing read permission or page-access.

Deliver back:
- Summary + files touched
- Migration name
- Confirm menu appears for Marketer/Finance Manager/Admin
