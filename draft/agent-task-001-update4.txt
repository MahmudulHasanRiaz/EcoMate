UPDATE 4 - Cutting invoice layout fix (final)

Scope: ONLY fix the issues listed here. No refactor.

1) Cutting invoice table columns are mismatched
File: src/app/dashboard/purchases/[id]/client-page.tsx
- In handleGenerateWorkOrder -> CUTTING itemsHtml:
  - Header still shows Qty/Jama/Orna/Selowar but row now outputs printingDamage/cuttingDamage instead of Qty.
  - Fix columns so headers match cells.

Expected layout (simple + space-safe):
- Keep headers: Product, Qty, Jama, Orna, Selowar, Cutting Cost.
- In Qty cell: show actual qty, and if cutting damage > 0 show a small line "Dmg: X" (same style as Printing invoice).
- Do NOT add extra columns for damage. (Printing damage can be omitted or shown as a compact line under qty only if you really want.)

2) "For X pcs" display condition
File: src/app/dashboard/purchases/[id]/client-page.tsx
- In cutting invoice line total, show the "For X pcs" line when (printingDamage + cuttingDamage) > 0 (not only cutting damage).
- Billable math already uses qty - printing - cutting; keep that.

Deliver back: summary of changes + tests run.
