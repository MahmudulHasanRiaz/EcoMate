UPDATE P34 - Webhook Failures UI (minimal, admin/manager)

Scope: UI only. Use existing APIs. Keep changes minimal. No refactors.

Goal:
- Provide a simple, safe UI to view webhook failures and replay them.
- Use existing endpoints:
  - GET /api/webhooks/failures
  - POST /api/webhooks/failures/[id]/replay

1) Add route + page
File: src/app/dashboard/webhook-failures/page.tsx (NEW)
- Basic page layout (Card/Table) with filters:
  - Source (text)
  - Integration ID (text)
  - Order ID (text)
  - Date From / Date To
- List columns:
  - CreatedAt
  - Source
  - IntegrationId
  - OrderId / ExternalOrderId
  - Error (truncate to 120 chars)
  - Actions: Replay button
- Pagination: cursor + Load More button
- Use existing API with fetch helper (create in services if needed).

2) Add service helper
File: src/services/webhooks.ts (NEW) OR extend existing services
- getWebhookFailures({ pageSize, cursor, source, integrationId, orderId, dateFrom, dateTo })
- replayWebhookFailure(id)

3) Menu link
File: src/app/dashboard/layout-client.tsx
- Add menu item "Webhook Failures" under Settings/Integrations (visible only if permissions allow).
- Permission gate: integrations read (or admin).

4) Access control
- In UI, if no permission, show a simple "You do not have access" state.
- Do NOT expose payload by default.

5) Task tracker
File: draft/task.txt
- Add entry under Done (Unverified):
  ID: P34
  Title: Webhook Failures UI (List + Replay)
  Status: DONE-UNVERIFIED
  Evidence: dashboard/webhook-failures page + services/webhooks.ts

Deliver back:
- Summary + files touched
- Screenshot or short description of UI

Notes:
- Keep UI minimal (table + filters + replay).
- Replay should show toast success/fail.
- Do NOT add DB/migration.
