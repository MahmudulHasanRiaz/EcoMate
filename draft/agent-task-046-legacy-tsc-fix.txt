UPDATE P46 - Legacy TSC cleanup (Next 15 params + typing + schema drift)

Scope: Fix remaining TypeScript build errors in one pass. Keep changes minimal. No refactors.

Goal:
- `npx tsc --noEmit` passes.
- Do NOT change runtime logic beyond typing/compat fixes.
- Keep edits small and scoped to listed files.

Step 0) Capture current errors (evidence)
- Run:
  npx tsc --noEmit | Tee-Object -FilePath draft/tsc-legacy-errors-latest.txt

Step 1) Next.js 15 dynamic params (Promise)
Update every listed file to accept params/searchParams as Promise and `await` them.
Pattern:
  export async function GET(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
    const { id } = await params;
  }
For pages with searchParams:
  export default async function Page({ searchParams }: { searchParams: Promise<{ ... }> }) {
    const sp = await searchParams;
  }
Files:
- src/app/api/accounting/accounts/[id]/route.ts (DELETE/PATCH)
- src/app/api/issues/[id]/route.ts (GET/PATCH)
- src/app/api/marketing/campaigns/[id]/route.ts (GET/PATCH)
- src/app/api/marketing/campaigns/[id]/orders/route.ts (POST/DELETE)
- src/app/api/marketing/campaigns/[id]/spend/route.ts (POST)
- src/app/api/notifications/[id]/read/route.ts (PATCH)
- src/app/api/webhooks/failures/[id]/route.ts (PATCH)
- src/app/dashboard/issues/[id]/page.tsx (params)
- src/app/dashboard/issues/page.tsx (searchParams)
- src/app/dashboard/partners/my/page.tsx (searchParams)
- src/app/print/expenses/page.tsx (searchParams)

Step 2) Prisma config TS error
File: prisma.config.ts
- Remove `seed` field (Prisma v6 config does not support it).
- If seed is needed later, keep it in package.json or a script (no change now).

Step 3) Clerk invitation list param rename
Files:
- src/app/api/auth/cleanup/route.ts
- src/app/api/webhooks/clerk/route.ts
Replace getInvitationList({ emailAddress: ... }) with getInvitationList({ query: ... }).
(See node_modules/@clerk/backend/... InvitationApi.d.ts)

Step 4) check-passing route type shadow + spread error
File: src/app/api/check-passing/route.ts
- Remove local `type CheckPassingSource = ...` (conflicts with Prisma enum).
- Build `where` as Prisma.CheckPassingItemWhereInput (not a plain object spread on a union).
Example pattern:
  const where: Prisma.CheckPassingItemWhereInput = { ... }

Step 5) StaffAuthResult guard (issues routes)
Files:
- src/app/api/issues/route.ts
- src/app/api/issues/[id]/route.ts
`auth` is a union. Only access `auth.staff` after `auth.status === 'allowed'`.
If denied, return the error response as already done.

Step 6) Email null -> undefined
Files:
- src/app/api/customers/route.ts
- src/app/api/customers/[id]/route.ts
Ensure email is `string | undefined` when constructing input:
  email: customer.email ?? undefined

Step 7) Staff payments account label
File: src/app/api/staff/[id]/payments/route.ts
- Replace ACCOUNT_LABELS.staffPayable with ACCOUNT_LABELS.salariesPayable
File: src/server/modules/accounting.ts
- Add label:
  salariesPayable: 'Salaries Payable'

Step 8) Marketing OrderStatus includes typing
File: src/server/modules/marketing.ts
- Define invalid statuses as OrderStatus[] to satisfy `includes`:
  const invalidStatuses: OrderStatus[] = [OrderStatus.Canceled, ...];
- Use invalidStatuses.includes(order.status)

Step 9) Staff route type guard
File: src/app/api/staff/route.ts
- Fix predicate mismatch: avoid using `.filter(isStaffMember)` on possibly null list.
- Safest: const items = Array.isArray(res?.data) ? res.data : []; return items;

Step 10) Tasks report staffId null
File: src/app/api/tasks/report/route.ts
- Ensure staffId is string|null (not undefined):
  const staffId = body.staffId ?? null;

Step 11) Placeholder data typing
File: src/lib/placeholder-data.ts
- Add missing fields required by types:
  * Expense: approvalStatus
  * Inventory: reservedQuantity
  * OrderLog: id and orderId
- Add OrderStatus keys Draft and Damaged to maps used (statusLabelMap/statusColorMap).

Step 12) Staff UI type fields
File: src/types/index.ts
- Add optional fields used by UI:
  invitedName?: string | null;
  invitedPhone?: string | null;
(Or update UI to not expect them; prefer adding to type.)

Step 13) Permissions object missing integrations
File: src/lib/permissions.ts
- Ensure `integrations` exists in EMPTY_PERMISSIONS and every role object (use NO_ACCESS or FULL_ACCESS as policy).

Step 14) Script type fixes
Files:
- scripts/backfill-finance.ts (catch param typed unknown)
- scripts/discovery.ts (catch param typed unknown)
Example:
  } catch (e: unknown) { const err = e as Error; ... }

Step 15) BullMQ worker null filter
File: scripts/bullmq-worker.ts
- Ensure filter expects string not null (e.g., use '' or skip field when null).

Step 16) Remove stray snippet files that break tsc
- Delete these files if they are not imported:
  src/server/modules/analytics-cache-part.ts
  src/server/modules/purchases.ts_snippet.ts
(If you must keep, add `// @ts-nocheck` at top.)

Step 17) Re-run tsc
- Run:
  npx tsc --noEmit
- Save output:
  npx tsc --noEmit | Tee-Object -FilePath draft/tsc-legacy-errors-after.txt

Deliver back:
- Summary of files touched.
- Both tsc outputs in draft/.
- Confirm remaining errors if any.
