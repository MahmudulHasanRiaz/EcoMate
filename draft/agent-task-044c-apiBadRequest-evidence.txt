UPDATE P44c - Fix apiBadRequest evidence claim (no walkthrough, add diff proof)

Scope: draft only. No code changes.

1) Create draft/p44-apiBadRequest-diff.txt
- Include:
  a) `rg -n "apiBadRequest" src` output (should be 0 matches)
  b) `rg -n "apiBadRequest" src/app/api/webhooks/failures` (0 matches)
  c) Snippet of current `src/app/api/webhooks/failures/[id]/route.ts` import line showing apiError
  d) Note: walkthrough.md does not exist in repo, so no update was made.

2) Update draft/task.txt
- Under P44 notes: remove mention of walkthrough.md update.
- Add evidence link to draft/p44-apiBadRequest-diff.txt

Deliver back:
- New draft/p44-apiBadRequest-diff.txt
- Updated draft/task.txt
- Confirm no code changes.
