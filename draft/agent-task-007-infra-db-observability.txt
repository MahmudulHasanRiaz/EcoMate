UPDATE P05 - Infra/DB scaling + Observability

Scope: ONLY infra/runbook + DB migration items. No feature changes.

1) PM2 config
- Add ecosystem.config.js with two apps:
  - web: next start (or node server) with max-memory-restart
    - instances: process.env.WEB_CONCURRENCY || 1
  - worker: node scripts/bullmq-worker.ts (single instance)
- Document start/stop/scale commands in docs/runbook.md (or draft).

2) BullMQ worker ops
- Ensure scripts/bullmq-worker.ts is runnable in production build.
- Add npm scripts: "worker" and "worker:prod" if missing.

3) Redis/PgBouncer
- Provide ops notes for Redis + PgBouncer (connection pool, max connections).

4) BRIN + concurrent indexes
- Add new Prisma migration with raw SQL:
  - BRIN index on Order.date, LedgerEntry.date, OrderLog.timestamp, InventoryMovement.timestamp
  - Use CREATE INDEX CONCURRENTLY where safe
- Document safe rollout steps (off-peak).

5) Partitioning (staged, no immediate data rewrite)
- Add runbook only (no live conversion now):
  - When row count/age threshold reached, convert Orders/Ledger/Logs to monthly partitions.
  - Use a scheduled maintenance window for table rewrite or pg_partman-based migration.
- Add a readiness check script that reports row counts + largest tables.

5) Observability
- Enable pg_stat_statements (migration or DB config) + note how to query top slow statements.
- Add lightweight request timing logs on heavy endpoints (orders, purchases, analytics) if missing.

6) API rate limiting
- Use existing Redis rate limiter util for admin/report endpoints.

Deliver back
- Summary + files touched
- Provide runbook snippet for VPS deploy (pm2 start/reload + worker).
