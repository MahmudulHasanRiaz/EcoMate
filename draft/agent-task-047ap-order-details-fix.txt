P47ap - Order Details + Incomplete Leads runtime fixes

Goals
- Fix Order Details page data shape (child orders + logs) if still broken.
- Resolve /api/orders/incomplete 500 due to WooCheckoutLead relation alias mismatch.
- Eliminate Prisma runtime errors:
  - AppSetting.upsert missing updatedAt
  - StaffIncome.createMany missing id

Scope
- prisma/schema.prisma
- src/server/modules/orders.ts
- src/server/modules/stock-sync.ts
- src/server/utils/staff-salary-accrual.ts (optional if schema fix applied)
- src/app/api/orders/incomplete/route.ts

1) Schema: fix StaffIncome id default (required)
File: prisma/schema.prisma
In model StaffIncome, ensure:
```
id String @id @default(cuid())
```
This allows createMany to auto-generate ids.

2) Schema: ensure WooCheckoutLead relation field names match code
File: prisma/schema.prisma
Confirm WooCheckoutLead uses lowerCamel relation names:
```
business    Business               @relation(fields: [businessId], references: [id], onDelete: Cascade)
integration WooCommerceIntegration @relation(fields: [integrationId], references: [id], onDelete: Cascade)
```
If you see Business / WooCommerceIntegration field names (PascalCase), rename them to business/integration.
(This is client-only; no DB migration needed.)

3) Prisma generate + (optional) migration
If you changed schema in step 1:
```
npx prisma migrate dev --name staffincome_id_default
npx prisma generate
```
Production: `npx prisma migrate deploy && npx prisma generate`.

4) Defensive fix for AppSetting.updatedAt (prevents runtime crash)
File: src/server/modules/stock-sync.ts
In setStockAuditLastRun, update create payload:
```
create: { key: STOCK_AUDIT_SETTING_KEY, value, updatedAt: new Date() },
```
This works even if the client still expects updatedAt explicitly.

5) Order Details data shape (only if still broken)
File: src/server/modules/orders.ts
In getOrderById include:
```
Business: { select: { name: true, logo: true, phone: true, address: true } },
other_Order: true,
OrderLog: { orderBy: { timestamp: 'desc' }, include: { staff: { select: { id: true, name: true } } } },
```
Then normalize:
```
if (order?.other_Order && !order.childOrders) order.childOrders = order.other_Order;
if (order?.OrderLog && !order.logs) order.logs = order.OrderLog.map(l => ({ ...l, user: l.staff?.name || l.user }));
```

6) Incomplete Orders API uses correct includes (verify)
File: src/app/api/orders/incomplete/route.ts
Ensure include block is:
```
include: { business: true, integration: true }
```
If Prisma errors persist after generate, re-check schema step #2.

Test
- Restart dev server (npm run dev).
- Load /dashboard/orders/all and /dashboard/orders/[id].
- Load /dashboard/orders/incomplete (no 500).
- Check logs: no AppSetting.updatedAt / StaffIncome.id errors.
