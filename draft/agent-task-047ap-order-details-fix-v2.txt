P47ap-v2 - Order Details still broken (timeline + safety + migration)

Goal
- Fix Order Details page if it still crashes or shows duplicate timeline.
- Ensure logs are always present and only shown once.
- Create migration for StaffIncome id default (required).

Scope
- src/app/dashboard/orders/[id]/page.tsx
- src/server/modules/orders.ts
- src/server/modules/issues.ts
- prisma/schema.prisma (migration)

1) Remove duplicate timeline card (UI fix)
File: src/app/dashboard/orders/[id]/page.tsx
Find both sections using <OrderTimeline ...>.
KEEP only one (recommended: "Order History" in the left column).
Delete the other "Recent Logs" card completely.

2) Ensure logs array always exists (defensive)
File: src/server/modules/orders.ts
In getOrderById, after serializeOrder:
```
if (normalized && !Array.isArray((normalized as any).logs)) {
  (normalized as any).logs = [];
}
```
Also keep the existing OrderLog -> logs mapping.

2.1) Fix OrderLog relation include (schema mismatch)
File: src/server/modules/orders.ts
In getOrderById include for OrderLog, replace:
```
include: { staff: { select: { id: true, name: true } } }
```
with:
```
include: { StaffMember: { select: { id: true, name: true } } }
```
Then update the mapping:
```
user: l.StaffMember?.name || l.user
```

3) Use logs safely in UI
File: src/app/dashboard/orders/[id]/page.tsx
Where OrderTimeline is used, always pass:
```
<OrderTimeline logs={order.logs || []} />
```

3.1) Fix Order Details "Issues" fetch crash (relation alias)
File: src/server/modules/issues.ts
The schema uses relation names StaffMember and Order (not assignedToUser/order).
Update includes in getIssues/createIssue/getIssueById:
- replace `assignedToUser` with `StaffMember`
- replace `order` with `Order`
Example for getIssues include:
```
include: {
  Order: { select: { id: true, orderNumber: true } },
  StaffMember: { select: { id: true, name: true } }
}
```
Then map:
```
orderNumber: issue.Order?.orderNumber || null
assignedToName: issue.StaffMember?.name || null
```

4) Migration for StaffIncome id default
File: prisma/schema.prisma
Confirm:
```
model StaffIncome {
  id String @id @default(cuid())
  ...
}
```
Then run:
```
npx prisma migrate dev --name staffincome_id_default
npx prisma generate
```

5) Restart dev server
```
npm run dev
npm run worker
```

Verification
- Open /dashboard/orders/[id].
- Page loads; timeline appears once.
- No console error for logs.
- Issues panel loads (no 500 from /api/issues).
