P47bs-delta2 - Final API error handling and void status polish

Scope
- `src/app/api/staff/[id]/fines/route.ts`
- `src/app/api/staff/[id]/fines/[fineId]/route.ts`
- `src/server/modules/staff-fines.ts`

1) Return JSON errors (not plain text)
- Replace `new NextResponse(message, { status })` with:
  `NextResponse.json({ error: message }, { status })`
- Apply to both POST and PATCH fines routes.

Reason:
- Frontend uses `handleApiResponse()` which expects JSON and reads `error`/`message`.
- Plain text causes generic `HTTP error!` and hides real message.

2) PATCH route: map known business errors to 400
File: `src/app/api/staff/[id]/fines/[fineId]/route.ts`
- In catch block, set:
  - status 400 for messages like:
    - 'already voided'
    - 'Fine not found'
    - 'Invalid action'
    - 'does not belong'
  - status 500 only for unknown errors.
- Return JSON `{ error: message }`.

3) Remove unused import
File: `src/server/modules/staff-fines.ts`
- Remove unused `getAccountIdByName` from import list.

Verification
1) Try voiding same fine twice:
- 1st void: success
- 2nd void: HTTP 400 + meaningful JSON error message

2) Try invalid staffId/fineId combination:
- HTTP 400/404 JSON error (not generic 500)

3) Confirm frontend toast shows backend error message clearly.
