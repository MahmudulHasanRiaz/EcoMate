P47bf - Purchase payment critical fixes + partner improvements (command file)

Decisions from owner:
- Supplier FIFO order: PO date oldest first
- Vendor FIFO order: ProductionStep.createdAt
- Check amounts count ONLY when checkStatus == 'Passed'
- Same person supplier+vendor => combined view
- Partner list 3-dot should have Record Payment
- Vendor can be multi-type (Printing + Cutting)

Do NOT mention git commit/push in responses. Just implement locally.

--------------------------------------------------------------------
1) Fix payment method mapping + check passing creation
--------------------------------------------------------------------
File: src/app/dashboard/partners/[id]/page.tsx
Location: PartnerPaymentForm submit handler

Change:
- If bank account selected and NOT check, method should be "Direct" (not "Bank").
- For MFS accounts keep: bKash, Nagad, Rocket.

Pseudo:
if (isCheckPayment) method = 'Check';
else if (name includes 'bank') method = 'Direct';
else if (name includes 'bkash') method = 'bKash';
else if (name includes 'nagad') method = 'Nagad';
else if (name includes 'rocket') method = 'Rocket';
else method = 'Cash';

File: src/server/modules/purchases.ts
Function: applyPartnerPaymentCore

Change:
- Treat cash-like methods: Cash, Direct, bKash, Nagad, Rocket as cash.
- Only Check method should set check fields.
- For check payments, set checkStatus = 'Pending' and create CheckPassingItem.

Implementation notes:
const isCheck = method === 'Check';
const isCashLike = ['Cash','Direct','bKash','Nagad','Rocket'].includes(method);
cash = isCash ? allocation : 0
check = isCheck ? allocation : 0
checkStatus = isCheck ? 'Pending' : undefined

After each purchasePayment.create (both Supplier + Vendor path), if check > 0:
  const checkItem = await buildCheckPassingItemFromPurchasePayment(tx, payment.id);
  if (checkItem) await upsertCheckPassingItem(tx, checkItem);

--------------------------------------------------------------------
2) FIFO order fix (Supplier uses PO date)
--------------------------------------------------------------------
File: src/server/modules/purchases.ts
Function: applyPartnerPaymentCore (Supplier path)

Change:
orderBy: { date: 'asc' } (optional tie-breaker: id: 'asc')

Vendor path: keep orderBy { createdAt: 'asc' }

--------------------------------------------------------------------
3) Only PASSED check counts everywhere
--------------------------------------------------------------------
Update all totals that add "check" to only include Passed.

Files to update (search "cash + check" in these):
- src/server/modules/purchases.ts (applyPartnerPaymentCore summaries, recompute helpers)
- src/services/partners.ts (getPartnerOverviewStats + getPartnerFinancials)
- src/app/dashboard/partners/[id]/page.tsx (financials + payment history calculations)
- src/app/dashboard/purchases/[id]/client-page.tsx (paid/due in invoice)

Rule:
const passedCheck = (p.checkStatus === 'Passed') ? (p.check || 0) : 0;
paid = (p.cash || 0) + passedCheck;

--------------------------------------------------------------------
4) Fix "Net Payable" double count in Purchase Details
--------------------------------------------------------------------
File: src/app/dashboard/purchases/[id]/client-page.tsx
Location: step invoice summary HTML for FABRIC/PRINTING/CUTTING

Current bug: partnerBalance already includes current bill, then adds current bill again.
Fix formula:
  totalBalance = partnerStat ? (partnerStat.totalTx - partnerStat.totalPaid) : 0;
  previousBalance = totalBalance - currentBill;
  previousDue = Math.max(previousBalance, 0);
  previousAdvance = Math.max(-previousBalance, 0);
  netPayable = Math.max(currentBill + previousDue - previousAdvance, 0);

UI:
- Show "Previous Due" or "Previous Advance" based on previousBalance.
- Show "Current Bill" and "Net Payable".

--------------------------------------------------------------------
5) Cancelled PO excluded from Vendor stats
--------------------------------------------------------------------
File: src/services/partners.ts
Function: getPartnerOverviewStats

Change productionStep.groupBy "where" to exclude cancelled:
where: {
  vendorId: { not: null },
  PurchaseOrder: { status: { not: 'Cancelled' } }
}

--------------------------------------------------------------------
6) Partner list 3-dot -> Record Payment
--------------------------------------------------------------------
File: src/app/dashboard/partners/page.tsx

Add menu item:
  <DropdownMenuItem asChild>
    <Link href={`/dashboard/partners/${partner.id}?pay=1`}>Record Payment</Link>
  </DropdownMenuItem>

File: src/app/dashboard/partners/[id]/page.tsx

Use useSearchParams and auto-open dialog:
  const searchParams = useSearchParams();
  useEffect(() => { if (searchParams.get('pay') === '1') setIsPaymentOpen(true); }, [searchParams]);

--------------------------------------------------------------------
7) Same person Supplier + Vendor (combined view) + Vendor multi-type
--------------------------------------------------------------------
Goal: Same person can be Supplier + Vendor. Vendor can be both Printing + Cutting.

Minimal implementation (no new tables):
- Allow Vendor type to be multi-select and store as CSV string "Printing, Cutting".
- Update all filters to use .toLowerCase().includes('print') / 'cut' (already mostly done).

UI changes:
File: src/app/dashboard/partners/page.tsx
  - Vendor type input: change to multi-select (checkbox or multiple select).
  - Persist as CSV string in payload.
  - Display: show "Printing / Cutting" (split CSV).

Also allow "Also create as Supplier/Vendor":
File: src/app/dashboard/partners/page.tsx
  - In add supplier dialog: checkbox "Also create Vendor". If checked, call vendor POST after supplier POST using same fields + selected vendor types.
  - In add vendor dialog: checkbox "Also create Supplier". If checked, call supplier POST after vendor POST.

Combined view (same person):
- Use name-based merge for now (best without schema change):
  In partner details, when computing combined due, if supplier and vendor names match, include both.
  In getPartnerOverviewStats, merge by name (supplierMap/vendorMap -> same key).

If you want stronger linking later, we can add partnerGroupId in schema, but NOT required for this batch.

--------------------------------------------------------------------
8) Encoding fix (Tk symbol)
--------------------------------------------------------------------
Replace all "৳" text literals in new UI additions with "Tk" to avoid encoding issues.

Files likely affected:
- src/app/dashboard/partners/[id]/page.tsx
- src/app/dashboard/partners/payment/[id]/invoice/page.tsx

--------------------------------------------------------------------
Quick local checks (optional)
--------------------------------------------------------------------
- Open Partner detail: payment dialog works, amount saves, check creates passing item
- New payment: Bank direct is treated as cash
- Summary shows previous due + current + net payable correctly
- Vendor type multi-select shows both
- Partner list 3-dot has Record Payment

