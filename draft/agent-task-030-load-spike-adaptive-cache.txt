UPDATE P30 - Load-spike adaptive throttling (minimal, safe)

Scope: report-cache + stock-sync only. Keep changes minimal. No refactors.

Goal:
- When the system is under load, temporarily increase report cache TTL and throttle heavy background sync jobs.
- Provide a simple on/off switch via Redis keys with TTL (no UI).

1) Add load-shed helper
Create: src/server/utils/load-shed.ts

```ts
import { getRedisClient } from '@/server/queues/redis';

const MEM_TTL_MS = 30 * 1000;
let cache: { reports: boolean; sync: boolean; expires: number } = { reports: false, sync: false, expires: 0 };

export async function getLoadShedFlags() {
  const now = Date.now();
  if (cache.expires > now) return cache;

  const redis = getRedisClient();
  let reports = false;
  let sync = false;
  if (redis) {
    try {
      const [r, s] = await Promise.all([
        redis.get('load-shed:reports'),
        redis.get('load-shed:sync'),
      ]);
      reports = r === '1';
      sync = s === '1';
    } catch {
      // fallback to false
    }
  }
  cache = { reports, sync, expires: now + MEM_TTL_MS };
  return cache;
}
```

2) Adaptive TTL for report-cache
File: src/server/utils/report-cache.ts
- Import getLoadShedFlags.
- Inside getReportCache<T>(), before using ttlMs:
  - If load-shed reports flag true, use ttlMs = Math.max(ttlMs, 10 * 60 * 1000) (10 min).
  - Keep existing stampede guard + jitter logic intact.

3) Throttle heavy stock sync on load-shed
File: src/server/modules/stock-sync.ts
- Import getLoadShedFlags from '@/server/utils/load-shed'.
- In the scheduled audit / fallback audit section (where STOCK_AUDIT_INTERVAL_MS and fallbackAuditInFlight are used):
  - If load-shed sync flag true, skip audit run and log: [LOAD_SHED] skipping stock audit.
- In bulk sync entrypoint (where BULK_SYNC_CONCURRENCY / AUDIT_CONCURRENCY are used):
  - If load-shed sync flag true, temporarily set concurrency to 1 (local variables only) and log.
  - Do NOT change constants globally; adjust local concurrency used in pLimit or loops.

4) Document toggle usage
Create: draft/load-shed-toggle.txt
Include:
- To enable reports throttling for 10 min:
  `redis-cli SET load-shed:reports 1 EX 600`
- To enable sync throttling for 10 min:
  `redis-cli SET load-shed:sync 1 EX 600`
- To disable early:
  `redis-cli DEL load-shed:reports load-shed:sync`

5) Update draft/task.txt
Add entry under Done (Unverified):
ID: P30
Title: Load-spike Adaptive Throttling (Reports + Stock Sync)
Status: DONE-UNVERIFIED
Evidence: src/server/utils/load-shed.ts, src/server/utils/report-cache.ts, src/server/modules/stock-sync.ts, draft/load-shed-toggle.txt

Deliver back:
- Summary + files touched
- Paste first ~20 lines of draft/load-shed-toggle.txt
- Note any runtime log line added

Notes:
- No UI changes.
- No Prisma changes.
- Keep code minimal.
