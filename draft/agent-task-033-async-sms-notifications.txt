UPDATE P33 - Enforce async SMS/Notifications (queue-first, safe fallback)

Scope: sms-notifications + notifications modules only. Minimal changes.

Goal:
- Ensure external SMS sends are asynchronous (queue) by default.
- Avoid blocking request threads with direct HTTP calls.
- Keep a controlled fallback for dev only.

1) SMS: queue-only by default
File: src/server/modules/sms-notifications.ts
- Update queueOrSendSms():
  - If enqueueSmsJob queued -> return { ok: true, queued: true }
  - If NOT queued:
      * If process.env.ALLOW_SYNC_SMS === 'true': sendSmsRaw (current fallback) and log warning.
      * Else: log error and return { ok: false, queued: false, reason: 'queue_unavailable' }
- Add log line when queue missing:
  console.error('[SMS_QUEUE_UNAVAILABLE] Falling back:', allowSync)

2) Notifications: queue-first + safe fallback (DB only)
File: src/server/modules/notifications.ts
- In queueOrCreateNotification():
  - If queued -> return { queued: true }
  - If NOT queued -> log warning and createNotification() (DB write only; no external call)
  - Add log: [NOTIFY_QUEUE_UNAVAILABLE]

3) Optional dev flag doc
Create: draft/sms-queue-fallback.txt
Include:
- Default: queue-only (no sync SMS)
- To allow fallback in dev:
  `setx ALLOW_SYNC_SMS true`
- Reminder: restart server after setting env

4) Update task tracker
File: draft/task.txt
- Add entry under Done (Unverified):
  ID: P33
  Title: Async SMS/Notifications queue-first policy
  Status: DONE-UNVERIFIED
  Evidence: sms-notifications.ts, notifications.ts, draft/sms-queue-fallback.txt

Deliver back:
- Summary + files touched
- First 10 lines of draft/sms-queue-fallback.txt
- Confirm logs added for queue unavailable

Notes:
- No schema changes.
- No UI changes.
- This aligns with plan: async external calls; safe fallback only in dev.
