UPDATE A02b - Remaining gaps after verification

Scope: ONLY fix items listed here. Keep changes minimal. No refactors.

Decision: Pending checks remain due. Do NOT use a separate Post‑Dated Cheques account.
Ledger posts check amount only when checkStatus === 'Passed'.

1) Check Passing UI build error (duplicate state)
File: src/app/dashboard/check-passing/client-page.tsx
Issue:
- `const [isHistoryLoading, setIsHistoryLoading]` declared twice.
Fix:
- Remove the duplicate declaration.

2) Check Passing search should include checkNo
File: src/app/api/check-passing/route.ts
Fix:
- Add checkNo to search filter (ILIKE) alongside referenceLabel/referenceId/payee/type.

3) Expense API missing checkNo passthrough
Files:
- src/app/api/expenses/route.ts
- src/app/api/expenses/[id]/route.ts
Fix:
- POST: include `checkNo` in createExpense payload.
- PATCH: include `checkNo` in updateExpense payload.
- GET (by id): include `checkNo` in response payload.

4) Purchase mapping missing checkNo for general + step payments
File: src/server/modules/purchases.ts
Function: mapDbPoToAppPo
Fix:
- Add `checkNo` to `generalPayment` object.
- Add `checkNo` to `productionPaymentMap[stepType]`.
Reason: after refresh, UI should retain checkNo for general + step payment forms.

4.1) mapDbPoToAppPo `payment` object also needs checkNo
File: src/server/modules/purchases.ts
Function: mapDbPoToAppPo
Fix:
- In the returned `payment` object (near the bottom), include `checkNo: generalPayment.checkNo || undefined`.
Reason: client uses purchaseOrder.payment to initialize general payment state.

5) Purchases list pagination stability (confirm)
File: src/server/modules/purchases.ts
Function: getPurchases
Status: orderBy date+id is set, cursor still id-only.
Fix (only if you can do safely now):
- Use composite cursor (date+id) OR keep as-is but verify no duplicates in list with identical dates.

Deliver back:
- Summary of changes + files touched
- Note any behavior changes or assumptions

6) Check Passing "View Voucher" eye icon should be visible in row (not only inside 3-dot menu)
File: src/app/dashboard/check-passing/client-page.tsx
Fix:
- Add a dedicated Eye icon button in the row action column (desktop table) and in the mobile card header.
- Clicking the Eye icon should open the voucher modal (setViewCheck(check)).
- Keep the kebab menu for status/history actions, but remove "View Voucher" from the menu to avoid duplication.

7) Purchases list crash: po.logs undefined in list mapping
File: src/server/modules/purchases.ts
Function: mapDbPoToAppPo
Issue:
- getPurchases list query does not include logs, so po.logs is undefined.
Fix:
- Change `logs: po.logs.map(...)` to `logs: (po.logs || []).map(...)`.
- This keeps list mapping safe without adding heavy includes.
