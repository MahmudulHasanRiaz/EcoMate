UPDATE 27 - Make barcode narrower without breaking scan

Scope: ONLY shrink barcode width. Keep logic minimal.

Goal:
- Barcode width should be narrower for long lot numbers, but still scannable.
- Keep value unchanged (do NOT truncate/modify lot number).

File: src/app/dashboard/purchases/[id]/client-page.tsx
Section: handleGenerateWorkOrder -> formatLots + waitForBarcode

1) Reduce barcode container size (visual)
- In formatLots, update the SVG style to smaller width/height:
  style="width:70px;height:10px;display:inline-block;"
  (If 70px still feels wide, use 60px; do not go below 50px.)

2) Dynamic module width based on value length (functional)
- In waitForBarcode, when calling JsBarcode(node, val, opts):
  - Compute a target max width (e.g. 70).
  - Estimate module count for CODE128:
      approxModules = Math.max(1, (val.length + 2) * 11);
  - moduleWidth = clamp(maxWidth / approxModules, 0.6, 1.0)
    (use min 0.6 to keep it scannable)
  - height: 10–12, margin: 0 or 1 (tiny quiet zone)
- Example:
    const maxWidth = 70;
    const approxModules = Math.max(1, (val.length + 2) * 11);
    const moduleWidth = Math.max(0.6, Math.min(1, maxWidth / approxModules));
    jsbc(node, val, { format:'CODE128', displayValue:false, height:10, width: moduleWidth, margin: 1 });

3) Keep safety fallback:
- If JsBarcode throws or val missing, hide svg (already exists).

Deliver back:
- Summary of changes
- Screenshot of invoice with shorter barcode (if possible)
- Note the final width/height used
