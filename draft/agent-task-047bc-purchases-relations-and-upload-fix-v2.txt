P47bc-v2 - Purchases relations cleanup + uploads persistence (local only)

Purpose
- Fix remaining Prisma relation name mismatches in purchases module.
- Ensure uploads persist and are writable in production containers.

Scope (coder local changes only)
- src/server/modules/purchases.ts
- src/services/partners.ts
- src/services/purchases.ts
- docker/entrypoint.sh (new or update)
- Dockerfile
- public/uploads/.gitkeep (new)

------------------------------------------------------------
A) Purchases: eliminate remaining camelCase relations
------------------------------------------------------------
Schema uses PascalCase relations:
- Supplier
- PurchaseOrderItem
- PurchaseOrderLog
- PurchasePayment
- ProductionStep
- FabricLotUsage
- Vendor
- ProductVariant
- InventoryItem
- PurchaseOrder (relation name on payment)

1) Global replacements in src/server/modules/purchases.ts
Replace ALL of these in includes, where clauses, and data updates:
- supplier -> Supplier
- items -> PurchaseOrderItem
- logs -> PurchaseOrderLog
- payments -> PurchasePayment
- productionSteps -> ProductionStep
- fabricLotUsages -> FabricLotUsage
- vendor -> Vendor
- productionStep -> ProductionStep
- variant -> ProductVariant
- inventoryItem -> InventoryItem
- purchaseOrder -> PurchaseOrder

2) Items count field
The scalar field is `items` (NOT itemsCount).
Update all of these:
- data: { itemsCount: ... } -> data: { items: ... }
- return mapping: itemsCount: po.itemsCount -> items: po.items
- stats sum: _sum: { itemsCount: true } -> _sum: { items: true }
- anywhere itemsCount variable is only used to compute the scalar: keep variable name ok, but write to `items`.

3) Fix remaining specific blocks (examples)
Search for these and change:
- include: { items: true, productionSteps: true }
  -> include: { PurchaseOrderItem: true, ProductionStep: true }
- include: { supplier: true }
  -> include: { Supplier: true }
- include: { payments: true }
  -> include: { PurchasePayment: true }
- include: { logs: { orderBy: ... } }
  -> include: { PurchaseOrderLog: { orderBy: ... } }
- include: { payments: { include: { vendor: true, productionStep: true } } }
  -> include: { PurchasePayment: { include: { Vendor: true, ProductionStep: true } } }
- include: { productionSteps: { include: { vendor: true }, orderBy: { stepType: 'asc' } } }
  -> include: { ProductionStep: { include: { Vendor: true }, orderBy: { stepType: 'asc' } } }

4) Update mapDbPoToAppPo in purchases.ts
- payments source: po.PurchasePayment (NOT po.payments)
- productionSteps source: po.ProductionStep (NOT po.productionSteps)
- logs source: po.PurchaseOrderLog (NOT po.logs)
- hasInternalFabric: use po.PurchaseOrderItem.*.FabricLotUsage
- return items: po.items (NOT itemsCount)

5) Update services
src/services/partners.ts
- items: po.itemsCount -> items: po.items
- ensure relations used are PascalCase (Supplier, PurchaseOrderItem, PurchaseOrderLog, PurchasePayment, ProductionStep, FabricLotUsage, Vendor, PurchaseOrder)

src/services/purchases.ts
- items: po.itemsCount -> items: po.items

6) Verify (must be clean)
Run and ensure zero hits:
  rg "supplier\\b|items:\\s*\\{|\\bitems\\b\\.|payments\\b|productionSteps\\b|fabricLotUsages\\b|logs\\b|vendor\\b|variant\\b|purchaseOrder\\b" src/server/modules/purchases.ts

------------------------------------------------------------
B) Upload persistence (production images 404)
------------------------------------------------------------
Goal: /app/public/uploads must be writable and persistent.

1) docker/entrypoint.sh (new)
Create or ensure file exists:
#!/bin/sh
set -e
UPLOAD_DIR="/app/public/uploads"
mkdir -p "$UPLOAD_DIR"
chown -R 1001:1001 "$UPLOAD_DIR"
exec su-exec 1001:1001 "$@"

2) Dockerfile (runner stage)
Add:
RUN apk add --no-cache su-exec
COPY docker/entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
ENTRYPOINT ["/entrypoint.sh"]

Remove or comment:
USER nextjs

3) Ensure uploads dir exists in repo
Create: public/uploads/.gitkeep

Note
- docker-compose already has the bind mount:
  ./public/uploads:/app/public/uploads
If missing, add it to app service.

------------------------------------------------------------
Local verification
------------------------------------------------------------
- Purchases page loads without Prisma relation errors.
- Overview stats show correct totals.
- Create a product with image, refresh page: image persists (no 404).

Do NOT commit/push. Owner will handle deploy.
