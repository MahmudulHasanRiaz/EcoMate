UPDATE 17 - Fix lots visibility + cleanup lot formatting (final polish)

Scope: ONLY items listed here. Keep changes minimal.

1) Lots not showing in Printing/Cutting/Finishing invoices
File: src/app/dashboard/purchases/[id]/client-page.tsx
Section: handleGenerateWorkOrder -> itemsHtml

Problem:
- `isInternalFabric` is currently `type === 'FABRIC' && ...`, so it's false for PRINTING/CUTTING/FINISHING.
- Result: lots line never renders outside FABRIC.

Fix:
- Keep `isInternalFabric` as-is for summary.
- Add a new boolean for lots, e.g.:
  `const hasInternalFabric = stepDrafts.FABRIC.fabricSource === 'INTERNAL';`
- Use `hasInternalFabric` (not `isInternalFabric`) to decide lots rendering in ALL steps.

2) Lots formatting still cluttered; needs compact grouping
File: src/app/dashboard/purchases/[id]/client-page.tsx
Section: formatLots helper

Fix:
- Aggregate by lot number (or inventoryItemId) and SUM per part.
  Example output:
  "Lots: LOT123 (J:2,O:1,S:0.5) | LOT124 (J:1.5,S:1)"
- Use ASCII labels J/O/S only.
- Use `|` between lots, and `,` between part segments.
- Only include a part when its total > 0.

3) Image URL normalization safety (optional if already ok)
File: src/app/dashboard/purchases/[id]/client-page.tsx

Fix:
- Wrap `new URL(...)` in try/catch; if it fails, skip <img>.
- Keep the existing onError hide behavior.

Deliver back: summary + quick verification
- Printing invoice shows lots line when internal fabric used.
- Lots line is compact and readable.
