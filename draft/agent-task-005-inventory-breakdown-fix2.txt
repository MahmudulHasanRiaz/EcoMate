UPDATE P03i - Inventory breakdown totals + variant header correctness + image URL parsing

Scope: ONLY fix items listed here. Keep changes minimal. No refactors.

Problems observed:
- Parent row "Details/Breakdown" shows variant header (e.g. ARS-1-42) and wrong totals.
- Breakdown list shows multiple rows for same location (not aggregated).
- Some images return 404 (likely variant image JSON string not parsed).

1) Ensure parent breakdown uses product-level selection (no variantId)
File: src/app/dashboard/inventory/client-page.tsx
Fix:
- Add helper: `buildProductItem(productId: string): InventoryItem | null`
  - Use productsFromItems map for name/sku
  - Use first item from `items` for other required fields (locationName/id may be dummy).
  - IMPORTANT: set `variantId` = undefined and `sku` = productSku or product sku.
- Use buildProductItem for parent "Details/Breakdown" action:
  - Replace `openDialog('breakdown', findItemForVariant(pid))` with
    `openDialog('breakdown', buildProductItem(pid))`
- For variant rows, keep `openDialog('breakdown', repItem)` (variant-specific).

2) Aggregate breakdown rows by location
File: src/app/dashboard/inventory/client-page.tsx
Fix in breakdown fetch (useEffect):
- After fetching `/api/inventory`:
  - Build a map { locationName -> totalQty } (sum quantities for same location).
  - Convert map to array and sort by locationName.
  - Set breakdownItems to aggregated rows.
- Optionally add a final total row if helpful (label "Total").

3) Parse variant image JSON to avoid 404
File: src/server/modules/inventory.ts
Fix:
- Add `parseVariantImage` similar to parseProductImage:
  - If variant.image is JSON array string, return first url.
  - Else return string.
- Use `normalizeUrl(parseVariantImage(row.variant?.image))` instead of normalizeUrl(row.variant?.image).
- Keep fallback to productImage.

4) Variant header label fallback
File: src/app/dashboard/inventory/client-page.tsx
Fix:
- In breakdown DialogDescription, if variantId exists:
  - show variantName if present
  - else show formatted attributes (formatAttrs)
  - else show variantSku
  - else "Variant"

Deliver back:
- Summary + files touched
- Manual verify:
  - Parent breakdown header shows product name only (no variant in header).
  - Breakdown totals equal sum of variants per location (no duplicate location rows).
  - Variant breakdown header shows name/sku/attrs (not blank).
  - Images no longer 404 for variants with JSON image strings.
