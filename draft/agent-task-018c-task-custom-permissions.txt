UPDATE P18c - Allow Tasks create for Custom/default permissions (policy alignment)

Scope: ONLY permissions defaults. No refactors.

Goal:
- User confirmed: even Custom role (and fallback default permissions) should be able to create tasks.

1) Update Custom permissions default
File: src/lib/staff-permissions.ts
- In customPermissions (Custom role), change:
  tasks: noAccess
  to:
  tasks: perm(true, true, true, false)

2) Update fallback defaultPermissions
File: src/lib/staff-permissions.ts
- In defaultPermissions, change:
  tasks: readOnly
  to:
  tasks: perm(true, true, true, false)

3) Update PERMISSIONS for Custom
File: src/lib/permissions.ts
- In PERMISSIONS.Custom, change:
  tasks: NO_ACCESS
  to:
  tasks: CREATE_READ_UPDATE

Deliver back:
- Summary + files touched
- Confirm: Custom users can create/update tasks, delete still blocked.
