UPDATE P16 - Fix Expenses page errors (confirmDelete + API 500)

Scope: ONLY expenses module + DB migration if missing. Keep changes minimal. No refactors.

Issue A: confirmDelete is not defined (runtime error)
File: src/app/dashboard/expenses/client-page.tsx
Fix:
1) Add a confirmDelete handler near confirmReject/handleMarkAsPaid:
```
const confirmDelete = useCallback(async () => {
  if (!deleteDialog.expense) return;
  setIsDeleting(true);
  try {
    await deleteExpense(deleteDialog.expense.id);
    setAllExpenses(prev => prev.filter(e => e.id !== deleteDialog.expense!.id));
    setSelectedIds(prev => {
      const next = new Set(prev);
      next.delete(deleteDialog.expense!.id);
      return next;
    });
    toast({ title: 'Expense deleted' });
    closeDeleteDialog();
  } catch (err: any) {
    toast({ variant: 'destructive', title: 'Delete failed', description: err?.message || 'Check server logs.' });
    setIsDeleting(false);
  }
}, [deleteDialog.expense, closeDeleteDialog, toast]);
```
2) Ensure <AlertDialogAction onClick={confirmDelete}> uses this handler.

Issue B: /api/expenses 500 (Internal Server Error)
Likely cause: DB schema missing Expense approval columns/enums.

Steps to confirm:
1) Hit API once to capture server error:
   - Open: /api/expenses?pageSize=1&includeTotal=true
   - Check server console for the actual Prisma error (missing column/enum).

If schema mismatch is confirmed:
2) Create migration for Expense approval fields (no data loss):
   a) Stop dev server.
   b) Run:
      npx prisma migrate dev -n add_expense_approval_flow --create-only
   c) If drift prevents step (b), use diff fallback:
      $stamp = Get-Date -Format yyyyMMddHHmmss
      $folder = "prisma/migrations/${stamp}_add_expense_approval_flow"
      New-Item -ItemType Directory -Path $folder | Out-Null
      npx prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/schema.prisma --script | Out-File -FilePath "$folder/migration.sql" -Encoding utf8
      npx prisma migrate deploy
   d) Run: npx prisma generate
3) Restart dev server.

Migration must include:
- Enum ExpenseApprovalStatus (Submitted/Approved/Rejected)
- Expense columns: approvalStatus, submittedById/Name/At, approvedById/Name/At, rejectedById/Name/At, rejectionNote, paidById/Name.
Schema already has these; migration should align DB.

Verify:
- Expenses page loads without 500.
- No "confirmDelete is not defined".

Deliver back:
- Summary + files touched
- If migration created: folder name + migrate status output
