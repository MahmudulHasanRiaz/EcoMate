UPDATE P03j-4 - Fix remaining leftovers (duplicate breakdown state + stray lots/breakdown refs)

Scope: ONLY `src/app/dashboard/inventory/client-page.tsx`. Keep changes minimal. No refactors.

Problems found (verified in file):
- Duplicate `breakdownData` state declared twice.
- `setBreakdownItems(...)` calls still exist (but state was removed) → compile/runtime error.
- Old effects for `mode === 'breakdown'` and `mode === 'lots'` still exist.
- `lotRows` and DialogContent class still reference `mode === 'lots'`.

Fix steps:

1) Remove duplicate breakdownData state
- Keep the first `const [breakdownData, setBreakdownData]` near the top of the file.
- Delete the later duplicate declaration (around where "Async Breakdown State" is defined).

2) Replace orphaned `setBreakdownItems` with `setBreakdownData`
- In the stock fetch effect (mode === 'stock'):
  - Replace `setBreakdownItems([])` with `setBreakdownData([])`.
  - Replace `setBreakdownItems(...)` with `setBreakdownData(...)`.
- Ensure no `setBreakdownItems` remains in the file.

3) Remove legacy mode hooks (lots/breakdown)
- Delete the useEffect that runs only when `dialogState.mode === 'breakdown'`.
- Delete the useEffect that runs only when `dialogState.mode === 'lots'`.
- Remove the `if (mode === 'lots' && item)` pre-seed block in `openDialog`.

4) Remove remaining "lots" mode branches
- Update lotRows memo:
  - Replace `(dialogState.mode === 'lots' || dialogState.mode === 'stock')`
  - With: `(dialogState.mode === 'stock')`
- Update DialogContent class:
  - Replace `dialogState.mode === 'lots' && 'sm:max-w-4xl'`
  - With `dialogState.mode === 'stock' && 'sm:max-w-4xl'`.

5) Sanity check (must pass)
- `rg -n "mode === 'lots'|mode === 'breakdown'|setBreakdownItems" src/app/dashboard/inventory/client-page.tsx`
  → should return 0 matches.

Deliver back:
- Summary + files touched
- Paste the rg output confirming no legacy refs remain.
