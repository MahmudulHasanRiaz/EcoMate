UPDATE P34b - Fix missing cn import in Webhook Failures UI

Scope: ONLY src/app/dashboard/webhook-failures/page.tsx. Minimal change.

Problem:
- Component uses `cn(...)` but does not import it. Build/runtime error likely: `cn is not defined`.

Fix:
1) Add import at top:
   import { cn } from '@/lib/utils';

2) No other changes.

Deliver back:
- Summary + file touched
- Confirm build no longer complains about cn.
