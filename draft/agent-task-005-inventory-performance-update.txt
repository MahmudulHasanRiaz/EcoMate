UPDATE P03b - Inventory performance fixes after audit

Scope: ONLY fix items listed here. No refactors.
Decisions: 
- “All” includes zero-qty items.
- Low‑stock threshold uses Settings value.
- Overview stats must be GLOBAL (not page-only).
- Audit page uses pagination.

1) Fix Inventory client runtime issues
File: src/app/dashboard/inventory/client-page.tsx
- Define a `fetchAllData()` helper and use it where currently called.
  - Should call fetchStats() + fetchInventory(true) and reset cursor as needed.
- Remove duplicate `productById` definition (currently defined twice).
- Remove stale comment blocks about “remove filteredInventory” etc.

2) Inventory list filtering & low-stock threshold
Files:
- src/services/inventory.ts
- src/app/api/inventory/route.ts
- src/server/modules/inventory.ts
- src/app/dashboard/inventory/client-page.tsx

Changes:
- Add `status` param (active | low-stock | all) and `lowStockThreshold` param.
- UI: pass statusFilter and lowStockThreshold to getInventory().
- API: parse `status` and `lowStockThreshold` query params.
- Server: in getInventoryPaginated, apply:
  - active: quantity > 0
  - low-stock: quantity > 0 AND quantity <= lowStockThreshold
  - all: no quantity filter (include 0)
- Ensure the `where`/filters are actually used (currently lowStock + where are computed but unused).
- Add orderBy tie-breaker for cursor stability:
  - product query orderBy: [{ name: 'asc' }, { id: 'asc' }]

3) Global inventory stats (server + UI)
Files:
- src/server/modules/inventory.ts
- src/services/inventory.ts
- src/app/api/inventory/stats/route.ts (if exists) or existing stats route
- src/app/dashboard/inventory/client-page.tsx

Changes:
- Fix invalid `_count: { result: true }` (use `_count: { _all: true }` or count()).
- Extend getInventoryStats to return:
  - totalItems (sum quantity)
  - totalCostValue (sum quantity * unitCost)
  - totalSaleValue (sum quantity * COALESCE(variant.salePrice, variant.price, product.salePrice, product.price))
  - potentialProfit = totalSaleValue - totalCostValue
  - active / lowStock / all (counts) with lowStockThreshold
- Use $queryRaw if needed for sale value; keep it filtered by search/location.
- UI overview cards must use these GLOBAL stats, not page-only items.

4) Audit page pagination
File: src/app/dashboard/inventory/audit/client-page.tsx
- Use cursor pagination (pageSize 50) via getAuditItems({ cursor, pageSize, search, locationId }).
- Add “Load more” (or infinite scroll) using nextCursor.
- Remove “fetch all items” assumption.

5) Purchases detail lot refresh should NOT pull 1000 lots
File: src/app/dashboard/purchases/[id]/client-page.tsx
- Update refreshInventoryLots():
  - Use `/api/inventory/lots` with pageSize=50 and (if available) productId/variantId/locationId of active item.
  - Do NOT load all lots.
- Keep scan fallback already present.

Deliver back
- Summary + files touched
- Manual checks:
  - Inventory overview shows global totals (not page-only).
  - Low‑stock filter respects settings threshold.
  - Audit page loads with pagination.
  - Lot selection still works; no 1000‑lot fetch.
