UPDATE P44 - Production readiness verification (final)

Scope: verification only. No code changes unless a new blocker is found.
Goal: ensure Redis/BullMQ path is live, migrations are in sync, build is clean, and smoke tests pass before production.

1) Redis + Worker live check (local)
- Ensure Memurai is running:
  Get-Service Memurai
- Verify Redis responds:
  & "C:\Program Files\Memurai\memurai-cli.exe" ping
  Expect: PONG
- Start worker (in a separate terminal):
  npm run worker
- In main terminal, check queue counts:
  node -e "require('dotenv').config(); const {Queue}=require('bullmq'); const url=process.env.REDIS_URL||'redis://127.0.0.1:6379'; (async()=>{ const q=new Queue('stock-sync',{connection:{url}}); const c=await q.getJobCounts(); console.log(c); const jobs=await q.getJobs(['completed','failed'],0,5); console.log(jobs.map(j=>({id:j.id,name:j.name,finishedOn:j.finishedOn,failedReason:j.failedReason}))); await q.close(); })().catch(e=>{console.error(e); process.exit(1);});"
- Save output to: draft/p44-redis-worker-verify.txt

2) Prisma migration status
- Run:
  npx prisma migrate status
- Save output to: draft/p44-migrate-status.txt

3) Prisma client
- Run:
  npx prisma generate
- Confirm success (no EPERM). If EPERM occurs, stop dev server and retry.

4) Build
- Run:
  npm run build
- Save output to: draft/p44-build.txt

5) Smoke tests (critical flows)
- Start dev server if not running:
  npm run dev
- Check endpoints (200 expected):
  GET /api/settings/integrations/woo
  POST /api/orders/import/woo
  GET /api/webhooks/failures?pageSize=20
  GET /api/analytics
  GET /api/accounting/ledger?pageSize=10
- Save a short log snippet to: draft/p44-smoke.txt

6) Update task tracker
- draft/task.txt: add P44 under DONE-VERIFIED
  Evidence: draft/p44-redis-worker-verify.txt, draft/p44-migrate-status.txt, draft/p44-build.txt, draft/p44-smoke.txt

Deliver back:
- The four draft files above
- task.txt updated
- Any blockers noted with exact error text
