P47 Review – Status after “Woo API refinements” claim

What looks DONE
- Woo API routes exist with Bearer API key auth (plugin-config, restriction-check, incomplete-orders, complete).
- /api/restrictions now has permission guards (orders read/create/delete).
- Block/Unblock UI added in Order + Customer pages.
- Phone normalization updated (now accepts 0 + 10 digits and 880 + 10 digits).
- Post‑order restriction creation exists in /api/woo/incomplete-orders/complete.

Still WRONG / Missing (must fix)
1) IP is STILL shown in Order UI (privacy violation)
   - File: src/app/dashboard/orders/[id]/page.tsx (shows raw IP + “IP: …”).
   - Requirement: do NOT show IP anywhere in UI. Only show Blocked/Unblocked status and actions.

2) Manual block scope/integration is still broken
   - /api/restrictions defaults scope=SITE and expects integrationId for site checks.
   - UI does NOT send scope/integrationId (customer page), and order page only sends integrationId but no scope.
   - Result: phone blocks won’t match restriction‑check in plugin (site scoped) OR may act global unintentionally.
   - Fix: always send scope; for PHONE use SITE + integrationId (or GLOBAL if user chooses). For IP ALWAYS GLOBAL (per requirement).

3) /api/woo/restriction-check uses wrong blockType
   - It sets blockType = phoneRestriction.scope (should be 'PHONE').
   - Fix: blockType should be "PHONE" or "IP" only.

4) Restriction‑check missing general settings fallback
   - Only uses integration.restrictionSupportPhone / integration.supportPhone.
   - Requirement: fallback to business.phone then app general settings phone/supportPhone.

5) Dedupe window not enforced on incomplete leads
   - /api/woo/incomplete-orders always upserts by fingerprint; ignores dedupeMinutes/debounceMs/retrySeconds.
   - Requirement: respect dedupeMinutes (only update within window; otherwise create new lead).
   - Provide instructions in config (plugin) and/or server enforcement.

6) Phone validation mismatch with regex
   - plugin‑config regex still requires 0 prefix, while normalize allows 10 digits.
   - Decide one rule and align both. Per requirement: accept 0XXXXXXXXXX OR 880/+880 + 10 digits; 10 digits without 0 should be invalid.

7) Post‑order restriction implementation is hacky
   - /api/woo/incomplete-orders/complete uses an “upsert” with invalid id.
   - Replace with findFirst (same targetHash + integrationId + scope + not expired) → update expiresAt, else create.

8) Unused duplicate module still exists
   - src/server/modules/woo-public.ts not used. Remove or refactor APIs to use it. Don’t keep two implementations.

9) /api/restrictions DELETE deletes ALL by hash (no scope/integration)
   - This may unblock other integrations unintentionally.
   - For SITE blocks, include integrationId in delete filter.

Action plan (fix these, then report)
A) Update normalizeBdPhone and plugin-config regex to the EXACT spec.
B) Hide IP in UI. Keep block/unblock by using hashed value in API.
C) Fix scope handling in /api/restrictions + UI payloads.
D) Fix restriction-check fallback + blockType.
E) Implement proper dedupe logic in /api/woo/incomplete-orders.
F) Replace the invalid upsert in complete route with proper findFirst/update/create.
G) Remove woo-public or route all APIs through it.

Deliverables
- List of files changed + short snippets proving each fix.
- Confirm plugin still NOT packaged; no changes to ecomdrive.

IMPORTANT
Do NOT edit the ecomdrive plugin. New plugin must be in draft/fashionary-woo-plugin/ with name “Fashionary”.
