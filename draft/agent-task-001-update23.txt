UPDATE 23 - Lots block: add Remaining qty + per-part totals row

Scope: ONLY items listed here. Keep changes minimal.

1) Add "Rem" column to lots grid
File: src/app/dashboard/purchases/[id]/client-page.tsx
Section: handleGenerateWorkOrder -> formatLots

Fix:
- Extend the lots grid header to include "Rem".
- For each lot row, show remaining quantity from inventoryLotMap:
  remaining = lot?.quantity ?? 0
- If lots are grouped by lotNumber, SUM remaining across matching lots.
- Format remaining as integer (no decimals).

2) Show per-part totals for the item (important)
File: src/app/dashboard/purchases/[id]/client-page.tsx

Fix:
- Update formatLots signature to accept totals:
  formatLots(allocations, totals?: { j: number; o: number; s: number })
- Add a "Total" row at the top of the lots block:
  "Total" in Lot column, then J/O/S totals (yards) and Rem as "-"
- Use item.jamaYards / ornaYards / selowarYards to build totals.
- Only render totals row when totals provided (always for three-piece items).

3) Update call sites
- Pass totals in all steps when calling formatLots:
  formatLots(allocations, { j: it.jamaYards, o: it.ornaYards, s: it.selowarYards })

Deliver back: summary + verify
- Lots block shows Total row + Rem column.
- Per-lot allocations clearly show J/O/S distribution and remaining.
