UPDATE P26 - Post-P25b pg_stat verification (confirm auth write reduction)

Scope: DB-only verification. No code changes.

Goal:
- Validate that StaffMember UPDATE / _StaffBusinessAccess INSERT frequency drops after auth caching.

Steps:
1) Reset stats:
   $env:PGPASSWORD = '123456'
   & "C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -d fashionary -h localhost -p 5432 -c "SELECT pg_stat_statements_reset();"

2) Simulate normal traffic (5–10 mins):
   - Refresh dashboard, orders list, staff page, inventory, expenses.
   - Re-login once (or open new tab) to trigger auth flow.

3) Capture report (save as new file):
   & "C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -d fashionary -h localhost -p 5432 -c "SELECT now() AS captured_at, calls, total_exec_time, mean_exec_time, rows, query FROM pg_stat_statements WHERE dbid = (SELECT oid FROM pg_database WHERE datname = current_database()) ORDER BY total_exec_time DESC LIMIT 30;" | Out-File -FilePath "draft\pg-stat-report-after-p25b.txt" -Encoding utf8

4) Quick diff summary (manual):
   - Compare counts for queries containing "StaffMember" UPDATE and "_StaffBusinessAccess" INSERT between old report and new report.
   - Note if calls dropped significantly.

Deliver back:
- draft/pg-stat-report-after-p25b.txt
- Short note: calls before vs after for StaffMember UPDATE and _StaffBusinessAccess INSERT
- Update draft/task.txt with P26 evidence (optional)
