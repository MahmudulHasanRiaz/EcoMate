UPDATE P14b - Fix prisma import in check-passing route (named export error)

Scope: ONLY src/app/api/check-passing/route.ts. Keep changes minimal. No refactors.

Problem:
Build/runtime error says named export `prisma` not found from '@/lib/prisma' in check-passing route.
All other modules already use default import.

Fix:
1) Change the import to default:
   From:
     import { prisma } from '@/lib/prisma';
   To:
     import prisma from '@/lib/prisma';
2) Remove the trailing comment about "Ensure correct import..." to avoid confusion.

Verify:
1) rg -n "import \\{ prisma \\} from '@/lib/prisma'" src/app/api/check-passing/route.ts
   - Expect 0 matches.
2) rg -n "import \\{ prisma \\} from '@/lib/prisma'" src
   - Expect 0 matches (optional).

Deliver back:
- Summary + file touched
- rg output
