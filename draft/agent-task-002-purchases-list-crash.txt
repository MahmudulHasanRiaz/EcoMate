FIX - Purchases list crash in production (logs undefined)

Goal: Ensure mapDbPoToAppPo does not call po.logs.map when logs are not included.

1) Verify code is updated (local)
File: src/server/modules/purchases.ts
Check line where logs are mapped:
Expected:
  logs: (po.logs || []).map(l => ({
    ...
  }))
If still `po.logs.map`, update it and commit.

2) Deploy/restart steps (server)
- Stop running server (pm2/next).
- Clear build cache: delete `.next` on server.
- Rebuild: `npm run build` (or `pnpm build`).
- Restart server process (pm2 start or npm run start).

3) Verify
- Open /dashboard/purchases.
- Confirm /api/purchases returns 200 and no SERVER_CORE_ERROR in logs.

Deliver back: confirmation of file content + deploy restart logs.
