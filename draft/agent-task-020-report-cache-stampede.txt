UPDATE P20b - Report cache stampede guard (in-flight + Redis lock)

Scope: ONLY src/server/utils/report-cache.ts. Keep changes minimal. No refactors in callers.

Goal:
- Prevent cache stampede across processes and dedupe concurrent same-key requests in-process.
- Keep behavior identical for callers.

Requirements:
1) Add in-flight coalescing (same process)
- Add: const inflight = new Map<string, Promise<unknown>>();
- If inflight has key, return the stored Promise.
- Ensure inflight entry is deleted in finally.

2) Add Redis lock (cross-process)
- Use a short lock TTL (e.g., 15s) with NX/EX or NX/PX.
- Lock key: `${key}:lock`.
- If lock acquired: run loader, cache result, release lock in finally (DEL key).
- If lock NOT acquired: poll Redis for the cached value (e.g., 5 tries x 200ms) and return if found.
- Fail-open: if still missing after polls, compute without lock (do not block).

3) TTL jitter to reduce synchronized expiry
- Apply jitter to ttlMs before caching:
  const jitter = Math.floor(ttlMs * 0.1 * Math.random());
  const effectiveTtl = ttlMs - jitter; // only reduce, never increase
- Use effectiveTtl for mem + Redis set.

4) Safe cleanup
- If mem cache entry is expired, delete it.
- Always clear inflight entry in finally.
- If Redis lock acquired, ensure DEL in finally.

Implementation notes (pseudo):
- Wrap the cache miss path in a promise assigned to inflight.
- Keep existing API: getReportCache<T>(key, loader, ttlMs?)

Deliver back:
- Summary + file touched
- Manual verify:
  - High-frequency hits for same key only compute once per process
  - Redis lock prevents multi-process stampede
  - No behavior change for callers
