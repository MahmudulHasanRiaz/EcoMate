UPDATE P22 - Async CSV export jobs for Orders (queue + download)

Scope: ONLY Orders export. Keep changes minimal. No refactors.
Goal: Replace client-side CSV export with server-side queued job (BullMQ reports queue) + download endpoint.

1) Prisma schema
File: prisma/schema.prisma
Add enums + model:
```
enum ExportJobStatus { Queued Processing Completed Failed }
enum ExportJobType { OrdersCsv }

model ExportJob {
  id          String          @id @default(cuid())
  type        ExportJobType
  status      ExportJobStatus @default(Queued)
  createdById String?
  businessId  String?
  params      Json?
  filePath    String?
  fileName    String?
  error       String?
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt
  completedAt DateTime?
}
```
Run migration:
```
npx prisma migrate dev -n add_export_jobs
npx prisma generate
```

2) Export job module
Create: src/server/modules/exports.ts
Functions:
- createExportJob({ type, params, createdById, businessId })
- markExportProcessing(id)
- markExportCompleted(id, { filePath, fileName })
- markExportFailed(id, error)
- getExportJobById(id)
- generateOrdersCsv({ orderIds, format, jobId })

Implementation details:
- generateOrdersCsv:
  - fetch orders by ids: id, orderNumber, customerName, status, total, date
  - header = ['OrderID','OrderNumber','Customer','Status','Total','Date']
  - rows map same as existing handleExport
  - CSV escape: double-quote and wrap
  - write file to `tmp/exports/orders-${jobId}.csv`
  - ensure directory exists: fs.mkdir(tmp/exports, { recursive: true })
  - mark job Completed with fileName `orders-${format}.csv` + filePath

3) Queue integration
File: src/server/queues/index.ts
- Add helper:
```
export async function enqueueReportJob(name: string, data: any) {
  const queue = getQueue('reports');
  if (!queue) return { queued: false };
  await queue.add(name, data, { attempts: 2, backoff: { type: 'exponential', delay: 1000 } });
  return { queued: true };
}
```

4) Worker handler
File: scripts/bullmq-worker.ts
- Replace reportWorker TODO with job handling:
```
const reportWorker = new Worker('reports', async (job) => {
  if (job.name === 'orders-export') {
    const { jobId, orderIds, format } = job.data;
    const { generateOrdersCsv, markExportProcessing, markExportFailed } = await import('@/server/modules/exports');
    await markExportProcessing(jobId);
    try {
      await generateOrdersCsv({ orderIds, format, jobId });
      return { ok: true };
    } catch (err) {
      await markExportFailed(jobId, String(err));
      throw err;
    }
  }
  return { ok: false, reason: 'unknown_job' };
}, { connection, concurrency: 2 });
```

5) API endpoints
Create:
- src/app/api/exports/orders/route.ts
  - POST: enforcePermission('orders','read')
  - body: { orderIds: string[], format: 'all'|'steadfast'|'pathao'|'redx' }
  - validate orderIds length >0 and <= 2000
  - create ExportJob (type OrdersCsv, params)
  - enqueueReportJob('orders-export', { jobId, orderIds, format })
  - return { jobId }

- src/app/api/exports/[id]/route.ts
  - GET: enforcePermission('orders','read')
  - fetch ExportJob; ensure createdById matches staff.id OR staff is Admin
  - return status + fileName

- src/app/api/exports/[id]/download/route.ts
  - GET: enforcePermission('orders','read')
  - validate job status Completed + filePath exists
  - stream file (Content-Type text/csv; Content-Disposition with fileName)

6) Orders UI (replace client export)
File: src/app/dashboard/orders/client-page.tsx
- Replace handleExport body with async flow:
  - determine orderIds (selected or filtered)
  - POST /api/exports/orders (format + orderIds)
  - toast: "Export queued"
  - poll /api/exports/{jobId} every 2s (max 60s)
  - when status=Completed => open /api/exports/{jobId}/download
  - on Failed/timeout => toast error
- Remove client-side CSV blob logic.

Deliver back:
- Summary + files touched
- Migration name
- Manual verify:
  - Export creates job, shows "queued" toast
  - When job completes, download starts
  - Non-owner cannot fetch job status
