UPDATE P12-3b - Fix marketing currency rendering (use Tk constant)

Scope: ONLY marketing UI files. Keep changes minimal.

Files:
- src/app/dashboard/marketing/page.tsx
- src/app/dashboard/marketing/campaign-details.tsx

Problem:
- UI still shows broken currency characters ("…3") even though currencyPrefix = 'Tk' exists.

Fix:
1) In marketing/page.tsx
   - Replace all literal currency fragments (…3) with `${currencyPrefix} `.
   - Targets include KPI cards, Campaigns table, Marketer Performance table.

2) In marketing/campaign-details.tsx
   - Add `const currencyPrefix = 'Tk';` near top.
   - Replace all literal currency fragments (…3) with `${currencyPrefix} `.
   - Targets include summary cards, Spend table, Orders table.

Deliver back:
- Summary + files touched
- Manual verify: no "…3" appears in Marketing dashboard or Campaign Details.
