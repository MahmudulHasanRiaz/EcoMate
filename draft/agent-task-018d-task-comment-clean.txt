UPDATE P18d - Clean outdated comment in tasks POST (no logic change)

Scope: ONLY remove stale comments. No refactors.

File: src/app/api/tasks/route.ts
Location: inside POST handler, above role/assignment logic.

Remove the outdated multi-line comment block that starts with:
"// Only Admin/Manager can create tasks"
and ends before:
"const body = await req.json();"

Do NOT change any logic.

Deliver back:
- Summary + file touched
- Confirm no logic changes
