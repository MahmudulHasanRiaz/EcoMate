P47cg - P47cf final closeout with strict scope isolation and zero-leftover guard

Goal
- Finish the remaining gap from P47cf so this pass is truly complete and safe to ship.
- Keep ONLY requested TS-fix scope in this pass.
- Prevent accidental/unrequested changes from leaking into this closeout.

Hard Rules
- Do not touch business logic outside the P47cf scope.
- Do not mix marketing feature work in this pass.
- No commit/push instructions in this task.

Allowed files for this pass
1) `scripts/check-data.ts`
2) `scripts/get-integration.ts`
3) `src/app/api/orders/incomplete/[id]/convert/route.ts`
4) `src/app/dashboard/partners/payment/[id]/invoice/page.tsx`
5) `src/app/dashboard/staff/[id]/page.tsx`
6) `src/app/track-order/[query]/page.tsx`
7) `src/server/modules/orders.ts`
8) `src/types/index.ts`

Optional dependency file (only if required to keep `tsc` clean):
- `src/lib/placeholder-data.ts`

If any other file must be touched, STOP and report first.

--------------------------------------------------------------------
1) Re-check and lock targeted fixes (no regressions)
--------------------------------------------------------------------

1.1 Prisma model usage in scripts
- Ensure BOTH files use `prisma.wooCommerceIntegration`:
  - `scripts/check-data.ts`
  - `scripts/get-integration.ts`

1.2 Auth narrowing in incomplete conversion route
File: `src/app/api/orders/incomplete/[id]/convert/route.ts`
- Keep strict guard:
  - `const auth = await getStaffAuthDetails();`
  - `if (auth.status !== 'ok') return apiError('Unauthorized', 401);`
  - then `const actor = auth.staff`

1.3 Partner invoice address typing
File: `src/app/dashboard/partners/payment/[id]/invoice/page.tsx`
- Do NOT access `payment.Vendor?.address` (Vendor has no address field).
- Keep partner address from valid source:
  - `payment.PurchaseOrder?.Supplier?.address || ''`

1.4 Staff page robustness
File: `src/app/dashboard/staff/[id]/page.tsx`
- No `staffMember.username` usage.
- Keep valid field (`staffCode` or `email`) for display.
- Keep safe date parsing pattern where needed:
  - `const d = new Date(value as any);`
  - `isNaN(d.getTime()) ? 'N/A' : ...`

1.5 Track-order map safety
File: `src/app/track-order/[query]/page.tsx`
- Keep:
  - `statusColors: Partial<Record<Order['status'], string>>`
  - fallback helper (e.g. `getStatusColor`) to avoid undefined class.

1.6 Orders relation include key
File: `src/server/modules/orders.ts`
- Ensure no invalid `logs: true` include remains.
- Use Prisma-valid relation key `OrderLog` where logs are included.
- Keep behavior unchanged other than relation-key correctness.

1.7 Types scope control
File: `src/types/index.ts`
- Keep required P47cf fix:
  - `statusBreakdown: Partial<Record<OrderStatus, number>>`
- IMPORTANT:
  - Do not add unrelated marketing type changes in this pass.
  - If marketing type edits already exist in this file, remove them from this P47cg closeout.

--------------------------------------------------------------------
2) No-scope-creep verification (mandatory)
--------------------------------------------------------------------

Run:
```powershell
mkdir -Force .tmp | Out-Null
git diff --name-only | Set-Content .tmp/p47cg_after_changed_files.txt
```

Then verify changed files are ONLY from the allowed list (plus optional `src/lib/placeholder-data.ts` if truly required):
```powershell
$allowed = @(
  'scripts/check-data.ts',
  'scripts/get-integration.ts',
  'src/app/api/orders/incomplete/[id]/convert/route.ts',
  'src/app/dashboard/partners/payment/[id]/invoice/page.tsx',
  'src/app/dashboard/staff/[id]/page.tsx',
  'src/app/track-order/[query]/page.tsx',
  'src/server/modules/orders.ts',
  'src/types/index.ts',
  'src/lib/placeholder-data.ts'
)

$actual = git diff --name-only
$unexpected = $actual | Where-Object { $_ -notin $allowed }
if ($unexpected) {
  Write-Host 'Unexpected files changed:'
  $unexpected | ForEach-Object { Write-Host " - $_" }
  exit 1
}
Write-Host 'Scope check passed.'
```

--------------------------------------------------------------------
3) Build + sanity checks (mandatory)
--------------------------------------------------------------------

```powershell
npx tsc --noEmit --pretty false
```

Focused grep:
```powershell
rg -n "wooIntegration" scripts/check-data.ts scripts/get-integration.ts
rg -n "logs:\\s*true" src/server/modules/orders.ts
rg -n "staffMember\\.username|\\.username" src/app/dashboard/staff/[id]/page.tsx
```

Expected:
- No `wooIntegration` in the two scripts
- No `logs: true` in `orders.ts`
- No `username` usage in staff page

--------------------------------------------------------------------
4) Clean output requirements (reply format)
--------------------------------------------------------------------

Reply with:
1) Exact files changed in this pass
2) `tsc` result (pass/fail)
3) Scope-check result (passed/failed)
4) Any remaining blocker (if any), with exact file and line

Do not claim completion without pasting command outputs summary.
