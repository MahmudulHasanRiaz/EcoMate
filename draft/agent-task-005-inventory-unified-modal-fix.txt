UPDATE P03j-2 - Fix unified "View Stock" tabs (remove legacy actions + correct Summary fetch)

Scope: ONLY inventory unified modal issues. Keep changes minimal. No refactors.

Context (current problems):
- Menu still shows "Details / Breakdown" and "View Lots" alongside "View Stock".
- Summary tab uses breakdownData, but fetch runs only when mode === 'breakdown', so Summary often empty/stale.
- DialogMode still includes 'breakdown'/'lots', and openDialog('lots') can open an empty dialog.

Goal:
- Single modal: "View Stock" with Tabs (Summary + Lots).
- Parent row shows ALL variants; Variant row shows ONLY that variant.
- Summary and Lots both work reliably.

1) Remove legacy actions
File: src/app/dashboard/inventory/client-page.tsx
- Parent dropdown: remove "Details / Breakdown" item entirely.
- Variant dropdown: remove "View Lots" and "Details / Breakdown" items entirely.
- Keep only "View Stock" (plus other existing actions like Adjust/Transfer, Movement, Print Lot Barcode).

2) Remove legacy dialog modes
File: src/app/dashboard/inventory/client-page.tsx
- Update DialogMode type to remove 'breakdown' and 'lots'.
- Remove any code paths that open 'breakdown' or 'lots'.
- Remove the useEffect that fetches breakdown only for mode === 'breakdown'.
- Remove the useEffect that loads lots for mode === 'lots'.
- Update DialogContent size class to use mode === 'stock' for wide layout.
- Update lotRows selector to only use mode === 'stock'.

3) Stock Summary fetch must run for mode === 'stock'
File: src/app/dashboard/inventory/client-page.tsx
- Create a single Summary fetch effect:
  if dialogState.isOpen && dialogState.mode === 'stock' && dialogState.selectedItem:
    - setBreakdownLoading(true)
    - reset breakdownData to []
    - fetch /api/inventory with:
        productId = selectedItem.productId
        variantId = selectedItem.variantId (only if present)
        locationId = (locationFilter !== 'all') ? locationFilter : undefined
        pageSize = 200 (or existing safe cap)
    - Aggregate items by locationName
    - setBreakdownData(rows)
    - setBreakdownLoading(false)
- Remove unused breakdownItems/breakdownRows state if it is no longer used.

4) Transparency for parent vs variant
File: src/app/dashboard/inventory/client-page.tsx
- When parent row opens stock:
  selectedItem.variantId must be undefined.
  Summary + Lots should include ALL variants.
- When variant row opens stock:
  selectedItem.variantId must be set.
  Summary + Lots should include ONLY that variant.
- Keep lots list showing variant label per row (formatLotProduct already includes variantName).
- In the dialog header/description, add a short scope label:
  - Parent: "All variants"
  - Variant: show variant name

5) Parent Summary should also show per-variant totals (transparent)
File: src/app/dashboard/inventory/client-page.tsx
- Only when viewing parent (variantId is undefined):
  - Compute variantTotals from the same /api/inventory items used for location summary.
  - Group by variantId (fallback to "No Variant" if missing).
  - Render a compact table below the Location summary:
    Columns: Variant, SKU (if any), Qty
  - Keep this section hidden for variant view.

Deliver back:
- Summary of changes + files touched
- Manual verify:
  - Only "View Stock" appears in menus.
  - Summary tab loads for parent + variant.
  - Parent shows totals across all variants.
  - Variant shows only its own totals.
