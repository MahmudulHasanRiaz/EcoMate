UPDATE P02b - Restore legacy dashboard folder + verify P02

Scope: ONLY restore deleted legacy files and verify. No refactors.

1) Restore legacy folder (accidentally removed)
- Restore `src/dashboard/**` from git history (or backup). Do NOT delete or modify its contents.
- If you already committed the deletion, revert that change only.

2) Confirm P02 behavior still intact
- Keep the new staff pagination endpoints and includeHistory default=false behavior as-is.
- Do not revert P02 feature changes.

3) Verification
- Run: `npx tsc --noEmit`
- Report the output (expect existing legacy errors; just record them).

Deliver back
- Confirmation that `src/dashboard` is restored.
- tsc output summary (do not fix errors in this step).
