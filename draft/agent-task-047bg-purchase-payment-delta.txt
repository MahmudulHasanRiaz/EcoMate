P47bg - Purchase payment delta fixes (apply on top of current changes)

Do NOT mention git commit/push. Just implement locally.

--------------------------------------------------------------------
1) Bank/Direct method fix (avoid zero-amount payments)
--------------------------------------------------------------------
File: src/app/dashboard/partners/[id]/page.tsx
PartnerPaymentForm submit handler:

Current:
  else if (name.includes('bank')) method = 'Bank';

Change:
  else if (name.includes('bank')) method = 'Direct';

Rationale: non-check bank transfer is immediate, should be treated as cash-like.

--------------------------------------------------------------------
2) applyPartnerPaymentCore: cash-like methods + check passing
--------------------------------------------------------------------
File: src/server/modules/purchases.ts
Function: applyPartnerPaymentCore

Add helpers:
  const isCheck = method === 'Check';
  const isCashLike = ['Cash','Direct','bKash','Nagad','Rocket'].includes(method);

When creating PurchasePayment (Supplier + Vendor):
  cash: isCashLike ? allocation : 0
  check: isCheck ? allocation : 0
  checkStatus: isCheck ? 'Pending' : undefined

After create, if check > 0:
  const checkItem = await buildCheckPassingItemFromPurchasePayment(tx, payment.id);
  if (checkItem) await upsertCheckPassingItem(tx, checkItem);

--------------------------------------------------------------------
3) Only Passed checks count in totals everywhere
--------------------------------------------------------------------
Update paid calculations to:
  passedAmt = (p.checkStatus === 'Passed') ? (p.check || 0) : 0
  paid = (p.cash || 0) + passedAmt

Files:
  - src/server/modules/purchases.ts
    * vendor path in applyPartnerPaymentCore: paid calc
    * getSupplierDueBalance / getVendorDueBalance
  - src/services/partners.ts
    * getPartnerOverviewStats (paymentStats aggregation)
    * getPartnerFinancials

--------------------------------------------------------------------
4) Exclude cancelled POs from vendor stats
--------------------------------------------------------------------
File: src/services/partners.ts
In getPartnerOverviewStats -> productionStep.groupBy:
  where: {
    vendorId: { not: null },
    PurchaseOrder: { status: { not: 'Cancelled' } }
  }

If Prisma groupBy rejects relation filter, fallback:
  - get allowed poIds from purchaseOrder where status != Cancelled
  - filter productionStep by poId in that list

--------------------------------------------------------------------
5) "Also create as Vendor/Supplier" should honor vendor types
--------------------------------------------------------------------
File: src/app/dashboard/partners/page.tsx

Current: when creating Supplier with alsoCreate -> vendor.type defaults "Printing".
Change:
  - When "Also create as Vendor" is checked, show Vendor Type multi-select even in supplier dialog.
  - Pass selectedTypes into otherData.type (CSV string) for dual create.
  - When creating Vendor with alsoCreate -> pass address field, but keep vendor types in vendor record (as now).

--------------------------------------------------------------------
6) Quick sanity checks (manual)
--------------------------------------------------------------------
- Record payment from partner list 3-dot -> modal opens -> payment saved -> amount non-zero.
- Bank + non-check payment posts as cash-like.
- Check payment creates pending check in check-passing list.
- Partner due totals ignore Pending/Bounced checks.
- Vendor stats ignore cancelled POs.

