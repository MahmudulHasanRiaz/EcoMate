# Claude — Profile Router

Pick the right profile:
- If you can **apply changes / run commands** (Claude Code CLI): read `agents/claude-code.md`
- If you are **chat-only** (Windows Claude App / VS Code extension): read `agents/claude-extension.md`

Shared directions live in: `agents/direction/*`
